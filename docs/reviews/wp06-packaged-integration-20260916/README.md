# PR #44: integrated packaged research and failure acceptance

## Fixed delivered version

- Previous main / PR45: `fff21287c0e3bfab2d679d7ac47bd8044f808ea9`.
- Original PR44: `3ecf4098f1a8cbf8f1fa078794887ae6bae59f41`.
- Final reviewed head: `1290d79935f2a5f343d217017bee2bc261efd47f`.
- Tested merge candidate: `bd039161c8466bda80b64f2a8740587f3739aead`.
- Actual merge: `4df08873f4b9b8f1378913c0d00ca6bb183e51e2`.
- Reviewed/tested/merged tree: `401f5a691e6898c670e446d142340d396862dae9`.
- Separate same-assistant reviews: 5218264160 and 5218344499.

Both original branches remain in the two-parent integration history. The final
merge tree was read back; no post-test source change or separate post-merge full
run is claimed. Seven feature paths, 695 additions and 2 deletions relative to
previous main. All production/script/SQL/dependency payloads remain unchanged.

## The actual packaged path

The existing distribution test continues inside its fresh SECOND extracted kit,
using that kit's Python, source and private native PostgreSQL. The reviewed
recipe is test-only code, not a shipped provider plugin, application or file queue.

Four distinct synthetic BTC/ETH requests form two batches. Two bounded turns span
an actual database stop/restart, with seven shared call reservations. Same-turn
replay makes no new client/call. Three completed research results and one failure
consume exactly 2+2+2+1 fake calls. Actual task commands retain two ready simulations,
one rejection and one not-simulated result, including denied permission, absent
receipt, identical replay and changed-input conflict checks.

After the declared real UTC minute closes, actual confirmation/evaluation commands
must match the packaged APIs. The original simulated cost-bound amounts are BTC
+2.971 and ETH -3.029 payout units. Failed/rejected attempts remain in the view;
actual_account_pnl is null. These are synthetic market inputs, clients and human
review assertions, not independently verified real forecasts, tariffs or earnings.

A separate real child exits86 after committing its original claim and one call
reservation. The claim remains incomplete, its allowance is not refunded, replay
constructs no new client, and strict current evaluation refuses incomplete history.
The pre-interruption historical view remains unchanged. Instance identity, stopped
state, immutable bundle and loaded project module origins are checked.

## New integrated proof: successful commit, failed output

For the first confirmation, actual inspection must prove the review is absent.
The real kit command then runs with ONLY its stdout sink changed. The sink requires
a successful confirmation status and emits a ten-byte prefix; the process must
return1. A separate real lookup must find the committed review/outcome. Explicit
SAME-input replay and another lookup must equal the full original receipt and
timestamps. No database or transaction is mocked, and no new review identity or
model retry is introduced. This verifies this injected failure, not every possible
crash, consumer acknowledgment or commit-timing guarantee.

## Separate self-review and retained failures

The initial integrated 14-case flow suite passed. Additional review cases first
produced 1 failed / 13 passed in a modeled Windows newline test: a ten-character
text prefix expanded to eleven bytes. The TEST sink now writes ten ASCII bytes
through the original stdout binary buffer. The failing assertion is unchanged.
This is a fixture portability defect, not a new production bug or a locally
executed Windows reproduction. False exit/prefix/stderr witnesses and timeout
preservation are tested; one representative Windows UTF16 command-line bound is
also checked, not a guarantee for arbitrary paths.

Final focused tests:154passed12.24s. Separate detached worktree:82passed4.24s,
comprising22flow cases and60unchanged PR45 regressions, source origins checked.
Exact complete local verification:37938passed/41skipped307.91s, with installation,
module/console and compilation checks. Local Python3.13.5/preinstalled tooling is
not hosted locked/Windows acceptance. The initial disposable editable-install
build-tool lookup failed; only that venv's tooling path was corrected, not project
dependencies. CodeGraph's directory exists but its executable is unavailable;
no CodeGraph execution/sync or fresh-agent/third-party audit is claimed.

All2922production/script/SQL/dependency hashes and67migrations equal previous main.
All72main and82original PR44 outer assertion nodes remain;84now. Native workflow
and existing process/job limits are unchanged. The original PR44 distribution
workflow additions are retained. Plan conflict resolution keeps PR45 section23,
appends original PR44 as section24, and records the combined proof. D1-D3/G6 and
real acceptance requirements are not lowered.

## Final-head hosted acceptance

| Gate | Actual final result | Run / job |
| --- | --- | --- |
| Locked full offline |37938passed,41skipped,350.98s|35052796019 /104656606466|
| Windows native PostgreSQL |1816passed,2skipped,0failures/errors,889.77s|35052796040 /104656606520|
| Existing paper/native |339passed,0skips/errors,420.26s|35052796048 /104656606560|
| Actual kit and integrated tests |145passed,0skips/errors,402.03s|35052796113 /104656606755|

All four workflows and source-cleanliness steps passed. Downloaded original
logs/JUnit, ZIP CRC, payload hashes and case-ID multisets were checked. All22flow
cases executed on Windows. Kit retains all123main cases and all137original PR44
cases, adding8review cases to the latter. Native/paper retain their original
case-ID sets. Repeated and overlapping counts must not be summed.

The existing365.139s native kit test includes BOTH prior lifecycle and the new
recipe, not just the new child duration. Its returned composition confirms four
attempts, four simulations, two settlements, seven original reservations, one
incomplete claim/one unrefunded reservation,184kit project modules, one confirmation
output failure and same_confirmation_replayed=true. Actual child exit86 and
immutable replay assertions passed. The180s watchdog diagnostic stack remains in
the raw log; it is not a failed process timeout, removed deadline or hidden rerun.
The inherited 'no models' log marker does not deny fake clients: no REAL provider
was called. No manual CI retry, prewarm or enlarged existing timeout occurred.

The41offline skips comprise18oldWindows shell cases, one dedicated PR42Windows
intentional-timeout case,18native opt-ins and four older optionalDB cases. The18
native opt-ins ran across native13/paper4/kit1 and18old shell cases ran. Five
optional checks remain unexecuted here: the dedicated PR42case and four olderDB
opt-ins. The two native Windows skips require privileged symlinks.

Final kit10429458995: inner66788538bytes, SHA256
`d43b9962f5a228365fc3aad61d1112a32a8ebc587f4d353334f9770865c7cedb`.
All2929manifest hashes/2928source payloads equal reviewed bytes. PG17.11seed1753
entries were checked for excluded fonts/cluster/passfiles. No engine executed
in container archive inspection; actual native execution was on isolated Windows.

## Publication, retention and remaining limits

Pinned-source helper35051758019/artifact10428883759 supplied6447main and6448old
PR44 blobs, verified with exact upstream commit reconstruction after clone DNS
failure. No actual user .env or credential was read. A fixed reviewed16474byte
patch was checked/applied to a deterministic combined baseline; SHA256
`9f43b384fea2f0586704551b161f28683cce25249cf2c6829b5e9938204012f0`.
A narrowly scoped transport job35052610990 published only the exact test/doc
objects to a NEW review ref, not main/the feature branch or workflow files.
Git-data publication reused the existing PR44 workflow blob and matched the
reviewed tree. The helper workflow/patch are not part of the application feature.
No platform write denial or force-push was routed around. Transport is not testing.

This evidence-only directory retains the report, source bindings, findings,
original review failure, detached result, final gate summary, raw checksums and
kit verification. Full local and final CI logs/JUnit, plus the old PR44kit proof,
are supplementary in pr44-integrated-review-evidence.zip. Its payload manifest,
CRC and outer hash/size are checked and recorded in the delivery comment. Do not
claim every raw log is stored in this directory. The included patch is historical
publication evidence of already-delivered work, NOT a task to apply to a user's
installation. No engine binaries, fonts, secrets or user DB are in the ZIP.
Actions artifacts expire after seven days; this is not an application Release.

No unresolved blocking finding identified in this scoped separate self-review
and actual tests. Same assistant, not external/fresh-agent audit or an absolute
no-defect guarantee. WP06PARTIAL/G6open/V1 1of6. D1-D3, actual input/fee/human/business
acceptance, safe release-specific data-version changes and the original PS5.1
intermittent cause remain open. No user-machine task, key discovery, user DB
operation, actual provider/market call, old-kit overlay or live order occurred.
