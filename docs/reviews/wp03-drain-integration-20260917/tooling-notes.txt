First static comparison call exceeded its 10-second tool deadline while invoking git show separately per protected file. No product test failed and no completion was claimed. Replaced per-file git processes with one ls-tree read and local blob comparisons; rerun is a static tooling check, not a CI rerun.
A second compound static/compile/local-commit/worktree command hit its 45-second tool deadline after creating the clean local candidate commit; no detached-worktree completion was claimed. A requested streaming test call returned StreamingExecNotEnabledContainerError before launching tests. Full verification is launched once below under a tracked local process.
The detached compound shell invocation returned status1 despite a complete 423-passing JUnit and before/after origin checks. Both git status rechecks returned0/clean. A separate direct interpreter invocation of the 36 drain/integration cases recorded pytest_return=0 and subprocess exit0; do not claim the earlier shell wrapper returned0. No source assertion was altered.
