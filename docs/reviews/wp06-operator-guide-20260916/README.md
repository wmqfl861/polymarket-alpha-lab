# PR #43: operating guide and own-source entrypoints

Evidence-only retention, not an installation, migration or local-agent task.

## Fixed delivered identity

- Base: `0c438098bab4d7c0a4f626664af43a31d96d8b08` (PR #42).
- Reviewed head: `673c022fd715b845c7a5c4ce054ff3e612df93f5`.
- Tested merge candidate: `40f0a060806a0f51582e36ce67b8b472e06acb74`.
- Actual merged main: `1ac3ec6b1ec0de18faa1e0227076b14075525ec7`.
- Actual merged/tested tree: `c8fcf787fed251375add47da3cbda54b4738eab8`.
- Separate same-assistant reviews: 5217224266 and final addendum 5217314693.

The actual merge tree was read back and equals the reviewed/tested tree.
No separately completed post-merge full run is claimed. The feature has one
commit, 11 changed paths, 546 additions and 7 deletions.

## Scope

The database and resolution-queue scripts now prefer their own adjacent source.
A missing or namespace-only selected package refuses execution instead of using
an unrelated editable installation. Original arguments, data-root semantics and
business operations remain. Resolution main is AST-equivalent after expanding
ROOT. This does not authenticate source or sandbox hostile/preloaded Python code.

The existing database/quickstart.md connects installation, discovery, approvals,
budgeted tasks, original-record inspection, canonical simulation input, manual
settlement, evaluation and cooperative restart. Fifteen literal examples are
checked by actual argument parsers but stop BEFORE business or network operations.
No new model client, file journal, queue, scoring system or execution route exists.

--root selects physical data/runtime/configuration AND its migration catalog,
not another Python version or automatic newer schema. A new extraction is a
separate empty instance, not an upgrade of old history. No old-kit overlay,
manifest editing, .local copying, forced recovery or general rollback is supplied.
Real provider, input-send permission and enforceable budget still require D1-D3.

The existing settlement runbook is newly included as optional kit documentation.
An unchanged PR38 kit with 2927 manifest entries still passes the current verifier;
its bytes were unchanged and no old engine was executed. The new kit test probes
both actual scripts with each of TWO extracted environments' own Python, unrelated
working directory and foreign editable-path decoy before initialization. Those
added probes use --help; the previous broader lifecycle assertions remain.

## Separate self-review and preserved failures

Original-script probes with the corrected valid fixture reproduced four failures.
An intermediate test also failed because its synthetic data root lacked required
layout metadata. That fixture issue was corrected separately and the ORIGINAL
scripts restored before the valid-fixture RED rerun. It is not a product defect.

Separate review of the initial path-only fix produced 4 failures / 2 passes:
missing or namespace-only own packages still fell back to unrelated code. The
explicit guard fixes this without weakening assertions. The guide link test
separately produced 1 failure / 16 passes for the omitted settlement runbook;
optional packaging fixes it. All original failure logs remain in the evidence ZIP.

Related tests: 332 passed in 32.33s. Final detached worktree: 27 passed in 23.95s,
clean. Exact final local full verification: 37856 passed / 41 skipped in 391.52s.
Local Python 3.13.5/preinstalled tooling is not hosted locked/Windows acceptance.
An earlier 526.28s full pass preceded one historical plan-word alignment and is
retained separately, not substituted for final-revision tests.

All 69 old kit assertion nodes, 67 SQL migrations, dependencies and 2900 protected
runtime/SQL/config/downloader payloads were preserved. The earlier count of 2899
excluded that last downloader file, not a changed payload. Distribution CI only
adds the new tests and routing; no old test, deadline, action or permission is
relaxed. Other workflows are unchanged. Its pre-existing path filter does not
separately name project_database.py: this candidate did trigger the actual kit gate
through its accompanying changes, not a certification of all future path filters.

Fixed source came from helper35037690766/artifact10424370865: 6440 archived blobs
verified against 6441 upstream entries after direct clone DNS failure. Local Git
history is reconstruction bookkeeping, not upstream commit identity. Public blank
examples were never activated. Disposable build-tool lookup, pre-pytest interruption
and unavailable streaming were recorded separately from product test results.

A detected plan upload mismatch led to the reviewed historical phrase shortening
from 本变更合并 PR to 本变更 PR, retaining the same evidence target. The final
local bytes and tests were aligned before publication. Other mismatched uploads
were unused. All 11 final hashes and reconstructed upstream tree match GitHub.

## Actual final-head acceptance

| Gate | Actual result | Run / job |
| --- | --- | --- |
| Locked full offline | 37856 passed, 41 skipped, 431.14s | 35039565562 / 104616182042 |
| Native Windows PostgreSQL | 1756 passed, 2 skipped, 945.43s | 35039565580 / 104616182170 |
| Existing paper/native | 339 passed, zero skips, 323.29s | 35039565573 / 104616182254 |
| Actual Windows kit and new tests | 123 passed, zero skips, 154.16s | 35039565613 / 104616182229 |

All four workflows and tracked-source cleanliness checks succeeded. Original
logs/JUnit, test IDs, ZIP CRC and file hashes were inspected. No failures/errors
occurred; native had no warnings. All 27 new cases executed on Windows: 4 entry,
6 review and 17 guide tests. Kit retains 96 prior IDs, native retains 1758 IDs and
18 original shell traces, and paper retains 339 IDs. The 105.985s native kit case
contains the added own-source probes. Counts overlap and must not be summed.

All 18 native opt-ins ran across native13/paper4/kit1, and the 18 old Windows shell
cases ran. Five optional checks did NOT execute this revision: PR42's dedicated
intentional Windows timeout case and four older optional database tests. Its
separate first-invocation matrix did not trigger. Native's two skips require
privileged symlinks. Diagnostic thread dumps and expected negative SQL stderr
remain raw evidence, not failed job timeouts. No manual CI rerun, prewarm, extended
limit or weakened assertion. The underlying PS5.1 intermittent fault is still open.

New kit artifact10425070802: inner 66787280 bytes, SHA256
`bdf9a4fd7c3c1642af538d451f0176468c0196ef46af6771f6d02ff46b40345d`.
All 2929 manifest hashes and 2928 source payloads match reviewed bytes. PostgreSQL
17.11 seed has 1753 entries checked for excluded fonts/cluster/passfile paths.
No engine ran during container archive inspection. Actual kit execution was in
Windows CI, not on the user's machine or against their business database.

## Retention and remaining gates

This directory retains six files: this report, review findings, source manifest,
raw checksums, final detached log and new-kit verification. Full initial/final
local and CI logs/JUnit are supplementary in conversation pr43-review-evidence.zip,
with a checked payload manifest and ZIP CRC. Its exact size/hash is in the delivery
comment. Do not claim this directory holds every raw log. Actions artifacts expire
after seven days, not a permanent Release. No runtime binaries, fonts, credentials,
user database or implementation patch are included in the supplementary ZIP.

No unresolved blocking finding was identified in this scoped self-review and
actual tests. This is a separate same-assistant review, not third-party/fresh-agent
audit or an absolute defect-free guarantee. WP06 stays PARTIAL, G6 open, V1 1/6.
D1-D3, real forecast/input/fee acceptance, release-specific safe data-version change
and original PS5.1 reliability remain open. No user-machine task, key request,
user-data migration, real model/market call, live order or old-kit overlay occurred.
