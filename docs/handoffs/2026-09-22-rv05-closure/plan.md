# RV05 收口与单次自动衔接开发计划

任务：PAL_RV05_CLOSURE_20260922；继续 PR #67 和原 RV05，不新计七天、不增加产品验收门槛。

## 1. 固定身份和事实分级

协调者从 GitHub 读取的真实候选为 `15f24d246493cdc114a127c366da537659d8c5d8`，完整树 `d388d113c0a75bb297bf1d60b13a9237fb31a07a`。用户转交的长 SHA 多出重复尾部，是转录差异，不是新候选。原观察仍是 `d929cc35214996a44008d4bbdd643bc8cc3550ea`。GitHub 主线/PR 现状须在写入前再读；只更新本批唯一草稿 PR #67，不合并 main、不强推。

四个当前 head 的工作流状态为 success：Windows soak contracts 35644035861、Native project distribution 35644035842、Research paper bridge 35644035963、Offline verification 35644035980。本轮只下载独立核验了 Windows soak artifact10660005886：ZIP9987字节，SHA256 fc9c6ce9abfb61c7c674ea86daad14d4f7e5e34c77508adfa1796a6f93ddd3e1，CRC正常，XML416passed/1skipped。唯一跳过为整个 `tests.test_rv05_n5_launch_review_rejection` 模块；两个Windows专属进程回收用例实际passed。没有重新执行产品CI/全量，没有据此声称完整PR已复审。

启动测试源码需要5个PAL_RV05_N5环境变量和未随PR交付的本地launch/review/rehearsal目录；缺失时模块级skip。因此本地39项通过仍算本地证据，不是干净Windows环境已经运行39项。当前PR文件清单仅含两个launch JSON，不含preflight-rv05.py、launch-rv05.ps1、run_final_review.py、README-LAUNCH.md。这是交付可复现性缺口，不否认本地文件存在。

完整11场景排程已独立枚举：种子2026092103，前864轮有74个ok-compute轮。每轮三族分配684/682/682，对应50616/50468/50468，共151552个计划槽位；同族索引域不重叠。本轮没有执行这些目标函数，也没有生成/审核实际回执。用户报告6144行试验，不等于151552行全部实跑；先复用任何尚未上报的完整证据，没有则按原RV05要求补有限完整执行，而不是增加新的十万门槛。

## 2. 本次收口原则

承认R1—R7、三族生产/审计、11+2和已有诊断工作已报告完成，不重新从零开发。冻结候选没有被证明存在新缺陷前，不为了忙碌改代码。新任务只补原计划未闭环的证据、可分发启动材料与无人消息时的单次衔接。复用已有工具；不要第四层报告器或新的多Agent框架。

逻辑节点可以使用现有获准并行能力；不要求新机器/账户/模型会话。没有现成额外Agent时单Agent轮转。每个文件一个owner，只有N0整合/推送。原正式长测与三进程不动；历史PID不足以识别当前所有权。新目录用独立源码/运行时副本，必要clone用--no-hardlinks；不写原候选、campaign、stop、watcher、状态或回执。

## 3. 六节点工作队列

| 节点 | 工作包/独占范围 | 现在执行 | 完成后继续 |
|---|---|---|---|
| N0 集成与版本 | 对应WP-02/03/06；任务状态、清单、PR | 校正完整SHA；登记既有产物/未提交修改；决定控制包与受测候选分别的身份，不无故解冻 | 调度READY任务和互审；维持一个草稿PR；每12小时汇总实际证据 |
| N1 完整有限容量 | 现有容量运行器及其测试；不改N2审核 | 对真实74轮、原round号、三族、2048总行执行生成→目标→oracle→原N2审核，跨轮去重；已有完整证据先复用 | 复核空间/时间、回执字节与失败分母；不重复正式72小时 |
| N2 可搬移交付 | 现有review源码、交付清单、N5测试和合成fixture；launcher写入归N3 | 把本地脚本去个人路径后发布固定GitHub提交；让39项拒绝测试从新checkout+合成fixture运行，不依赖旧会话目录 | 在原Windows专项CI补真实选择和零N5模块跳过断言；保留其他合理平台skip |
| N3 单次衔接 | 复用启动器的有限等待/claim控制，不写主driver | 查清是否已存在持久衔接进程；有则验证不重复；没有则实现薄等待模式、测试、准备arm | 到窗口自动重验旧退出/清理和新GO后最多一次启动；不要求用户再说发射 |
| N4 业务边界覆盖 | 对应WP-04/05现有测试，只补明确缺口 | 先只读确认partial/empty fill、过期/错源确认、失败研究不得结算等具体路径是否已有独立控制 | 已覆盖则DONE；有缺口才最小补测。F2满成交族不能证明未成交路径。不得为凑量再复制42组合 |
| N5 操作交付与终审 | 对应WP-06；既有说明/汇总，不新造框架 | 对照现有入口整理合成演练步骤、错误/未知口径及来源；检查包源码/运行材料差异和现有验收清单 | 原/新终态后分开审核，运行长测后的两类故障合同，准备准确的最终草稿报告 |

N1/N2/N3属于原RV05启动前闭环。N4/N5的额外覆盖/文档不自动成为新的长测门槛；若发现真实安全或结果正确性缺陷，则保留最小反例、NO_GO并明确修复版本影响，不可为了时间偷偷忽略。

## 4. 容量验证的固定口径

使用现有 `launch/manifest-rv05-final.json`，保留74个真实选中round号和对应sub_seed；不取任意74轮、不改种子/名称/族分配。6144试验及计划151552分别记录。允许在独立capacity目录无300秒等待地执行有限74轮生成/审核，这是覆盖预检不是第二份正式长测。顺序、资源受控，不启动完整soak、不重复pytest全部场景。

必须记录planned/generated/attempted/completed/oracle_passed/distinct_qualified，各族单列；所有目标调用必须实际发生。独立审计使用既有代码，不把生产函数当oracle，不以索引不交叉代替逻辑输入去重。只存规范输入/结果摘要和必要合成失败样本，不上传整个回执目录。失败停止受影响容量工作、保留首败，不同SHA修复后新结果单列。若已有相同候选/配置的完整实测，仅检查身份和证据，不再跑一次。

正式容量结论与正式72小时仍分开。三族内部时间/授权字段可保证不同输入，不表示覆盖同样多的不同逻辑分支，更不是策略有效样本。F2当前常数族只产生完整成交，已在ERRATA说明；未成交覆盖只能引用实际运行的其他对应测试。

## 5. 启动交付与可复现N5

不要把现有本地39项证明改写为CI39passed。N2只读确定脚本原文件（launcher代码只由N3修改）及其已绑定hash，复制到新可交付位置；本地路径以参数或模板表达。不能提交真实运行目录、PID路径、凭据或整个历史fixture。最小合成fixture要在pytest tmp_path中生成。无N5业务布局时必须验证测试集合完整，不再整模块skip；特定Windows用例可在Linux保留准确skip，并在Windows实跑。

允许在现有Windows-soak-contract工作流增加这些文件的路径触发和测试选择/必要合成env构造，不改原权限、timeout、依赖或其他门禁。不要新增CI重复全套。产物必须有固定commit+大小/SHA256清单、下载与--help/安全预检命令。与受测候选是两种身份：控制包变化不应仅因提交号不同就迫使重跑全部目标，但任何实际被测代码改变都需要新冻结与最终head适用门禁，旧soak时长不能移植。

## 6. 无需用户到点唤醒，但不虚构后台

本提示词确认：在原已批准边界内，允许现在部署并武装一个有限的单次衔接等待模式；到窗口条件全满足时无需再等用户一句“发射”。它不是启动真实模型的批准。优先复用现有launch-rv05能力，没有才薄扩展，不创建服务/计划任务，不建通用调度器，不用额外模型账户。

状态为UNARMED→ARMED_WAITING→PRECHECK→CLAIMED→STARTED→FINAL_REVIEW_READY；并列保留CANCELLED/EXPIRED/NO_GO/UNKNOWN。不是每个NO_GO都结束：仅原运行未结束/时间未到为可等待；identity漂移、clearance缺失、证据损坏、空间不足等为明确阻断，按实际类型处理，不循环刷新成GO。

绑定字段至少含批准的候选/tree、控制包/清单/生成器/合同与ERRATA摘要、运行时/依赖、原和新campaign的绝对路径、唯一claim路径、start window和deadline、预期固定argv以及STOP路径。拒绝BIND模板与错误/重复/未知字段；检查读写范围。进程身份不能只用裸PID；权限拒绝、已复用或无法确认保持UNKNOWN，不接管/杀死旧进程。

先做有限合成测试：未到时刻、原存活、原失败但正常退出、残留孩子、未知权限、PID复用、原快照半写、错误新hash、claim竞争、claim后崩溃、启动后ack丢失、操作者撤销、时钟跳变、睡眠越窗。二次实例/确认丢失不得第二次启动。停止发射控制不等于停止旧soak。部署后必须记录自己的PID+创建标识、命令、配置摘要、第一条ARMED_WAITING回执和撤销方法；若只有文件则NOT_STARTED，不声称已arm。

窗口：最早2026-09-24T02:14:50Z且旧运行实际退出/清理/证据固定；最晚2026-09-24T12:36:41Z；总截止2026-09-28T00:36:41Z。没有新任意加长等待、没有第三次长测。新72小时259200秒/864有效passed轮/100000实际不同合格输入各自判定；开始前完整预检及原适用门禁必须真实通过。

独立代码/文档节点只读或在其他目录工作，不改武装包/被冻结源。修改绑定对象先撤销未启动的arm并保留历史，再验证新对象；一旦已claim则不得擅自重arm/补发。新run发生非预期failed/unknown/interrupted、证据或身份异常即按原策略安全停止，不自动重跑。最终化只写脱敏摘要与READY，不会唤醒LLM/自动推送/合并。无法持久运行则明确能力阻断和待执行命令，不反复把用户催办当设计。

## 7. 每日工作与边界

09-22至09-23：N1有限全容量、N2发布可复現包、N3有限衔接测试与arm；N4/N5互不冲突推进。09-24窗口：有限控制完成旧终态核验与新单次启动；N0记录真实启动证据。09-24至09-27：原定正式观察、低负载只读进展、已有测试覆盖核对/操作文档，无第四层工具和重复长测。09-27至09-28截止：原和修正版分别结论、第二次故障合同、分离自审、PR67最终报告。

原总资源目标保持：合计工作集2GiB、相关新目录4GiB、证据512MiB、可用空间至少10GiB。初始最多一个新增合成测试worker，Agent逻辑并发按既有获准容量，不新增主机/认证。主任务压力大先暂停旁路。新容量原始/重复副本都计空间；测不到标未测，不能保证硬限额。不改电源策略。

已有R1—R7/六节点/39k全量/kit历史不重做。N0每2小时检查点、12小时总结新增事实，节点完成就转已有READY任务或互审；所有可做目标完成可提前收尾，不以最大天数为KPI。无新安全/正确性缺陷就不强行解冻，不借小改动反复重启72小时。

禁止真实CLI、凭据/OAuth、用户业务库/原生DB本机测试、真实行情/订单、Sandbox/BIOS/系统策略/ACL/服务/计划任务、安装或全局uv改动；旧业务根和旧准备包只按原允许的指定材料读取。旧kit唯一诊断rerun不新增许可；所有新候选final gates保留真实首败。源开发无需用户重复授权；真正越界事项单独WAITING_OWNER但不阻塞其他节点。

最终仅一个草稿PR67，发布文件不等于合并；新代码与旧观察分开。必须明确哪些是协调者已核验、哪些本地报告、哪些未执行。official_cases_run=0、sandbox_started=false、activation_authorized=false；WP-01/G1仍是唯一已完整验收包，不能用输入数或运行天数宣布G2—G6完成。
