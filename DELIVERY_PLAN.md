# V1 交付总计划：BTC／ETH 研究与模拟验证版

状态：**WP-01 的 G1 候选到待审入口已验收；其余五包未完成；不是 V1 发布完成声明。**
计划基线：`bd8f3c50bef29f0b3c64a260bf99f23d739684f4`（PR #26）。
基线日期：2026-09-14。用户已同意先收敛 BTC／ETH 首版，再扩展其他团队。
维护规则：每个相关 PR 同步更新本文件的状态、缺口、依赖及固定版本证据。

## 1. 唯一的当前交付口径

本文件是当前 V1 的范围、工作顺序和结束条件的唯一总表；它不是又一个独立报告模块。
`AGENTS.md` 仍管理工程、安全及授权约束。旧路线图继续作为长期方向和历史依据，
但旧的 Supabase 部署要求、外部审核要求、仅 BTC 可运行等描述不能覆盖当前项目规则。
本计划不授权读取密钥、付费模型调用、用户业务库写入或实盘交易。

**V1 的终点：** 普通用户在 Windows 上通过一套明确入口，完成受支持 BTC／ETH 市场的
筛选、规则审阅、获准模型研究、记录保存、任务运行管理、人工核验结算和成本感知模拟评估；
能识别失败与未知状态，能停止及重启且不丢失历史，能安装一个固定版本的可追溯发布包。
“输入 prepared”“代码合并”“测试全绿”都不单独等于这条完整路径已验收。

项目只有一个本期产品里程碑：**V1-Research-Paper**。下面 WP-01 至 WP-06 是六个工作包，
不是六次对话、六个文件或六个 PR；不再把每个查询器或修复称为一个新的产品阶段。

## 2. 首版范围与明确不做的事

| 纳入 V1 | 留到下一版或单独授权阶段 |
| --- | --- |
| `crypto_btc`、`crypto_eth` 两个团队；复用现有通用研究循环 | 另外八个团队的领域采集、连续运行及真实业务验收；十团队长期目标保留 |
| 显式 Yes/No、受现有合约形态和实际观察时间门禁支持的固定时点价格事件 | 路径依赖触价、期间最高／最低、区间统计、未知或矛盾规则；不为凑候选放宽门禁 |
| 项目私有原生 PostgreSQL、现有迁移账本、Windows 本地使用 | Docker、Supabase 服务、共享外部数据库、其他耐久业务存储 |
| 人工审阅规则、显式批准研究输入和模型；有界任务运行 | 自动批准模型输出、自动认证来源或链上最终性、任意工具／网址／代码执行 |
| 真实研究接入、失败留存、结算证据、模拟成交成本与评估 | 实盘、账户／钱包、签名、真实订单、自动仓位或资金分配 |
| 一个统一操作流程；优先复用现有命令行和组件 | 新 Web 仪表盘、重复健康报告／门禁／摘要层、框架重写和微服务拆分 |
| Windows 可追溯安装与保全旧数据的版本切换说明 | 原地覆盖旧 kit、复制 `.local` 冒充迁移、自动删库修复、无条件自动升级器 |

来源适配是准入条件而不是口头免责声明：Coinbase／Kraken 的 USD 小时行情只能作为研究输入，
不能当成 Binance USDT 分钟结算数据。规则审阅和结算核验必须明确引用合约实际指定的来源、
交易对、时间和粒度。此版允许人工独立取得结算证据；不要求先开发所有结算源的自动历史采集器。
无法核验的事件保留未决，不算已结算，也不纳入“已完成样本”。

## 3. 已有能力不是从零重写

| 可复用基础 | 基线证据／使用说明 | 不能由此推导的结论 |
| --- | --- | --- |
| 项目私有数据库启停、迁移、备份与权限控制 | [database/README.md](database/README.md)、[quickstart](database/quickstart.md)；PR #10—#14 | 用户业务库已升级、已有真实预测或新 V1 已发布 |
| 公开发现、双源预览、合约／观察时刻检查 | [发现](docs/research-crypto-discovery.md)、[启动](docs/research-crypto-launch.md)、[合约](docs/research-crypto-contract-scope.md)、[时刻](docs/research-crypto-observation-time.md)；PR #16—#22 | 第一个搜索结果可研究、来源等价、当前结果已获批 |
| 通用有界模型／工具循环、批量并行及任务收录 | [研究 Agent](docs/team-research-agent.md)、[启动](docs/research-crypto-launch.md) | 十团队真实运行完毕、常驻调度已完成、提供商已选定或已获付费授权 |
| 任务查询、完整有界清单及评估入口 | [查询](docs/research-execution-inspection.md)、[清单](docs/research-execution-inventory.md)、[评估](docs/research-evaluation-console.md)；PR #23—#25 | 查得到记录就能自动重跑、无记录等于全库为空、没有未完成任务等于预测有效 |
| 未决市场清单、候选证据、人工确认和单条结算查询 | [清单](docs/research-resolution-queue.md)、[确认](docs/research-resolution-review.md)、[查询](docs/research-resolution-inspection.md)；PR #13、#15、#26 | 自动确认最终性、旧候选被后来结果升级、真实结算全链路已验收 |
| 模拟成交、成本、风险和验证规范的既有组件 | [验证门槛](docs/research/validation-gates.md)、[长期路线](docs/superpowers/specs/2026-06-13-automated-investment-roadmap.md) | 这些组件已接入新研究主流程，或已有可信收益证据 |

证据分类必须保留：`代码已合并`、`离线验证`、`隔离原生集成`、`真实公开输入`、
`用户报告的本机检查`、`获准真实模型`、`完整业务验收`。一种不能替代另一种。
PR #26 的全量、Windows 和 kit 测试是组件工程证据，不是六个工作包的完成证明。

用户此前报告本机研究清单为零，之后报告了公开预览成功及受控网络重试恢复；
本计划没有重新查询其机器，也不把这份历史报告当成数据库“现在仍为空”的实时结论。
此前未发布的候选筛选器／模型预检只算未交付候选，不计入 main 的能力或本期完成量；
曾被平台拒绝的写入不得通过换接口、编码或代传绕过。

## 4. 六个工作包总验收表

状态词：`NEXT` 下一实施项；`PARTIAL` 有基础但未完成；`AWAITING_OWNER` 缺用户决定；
`IN_PROGRESS` 正在实施；`WAITING` 等待真实事件或证据；`BLOCKED` 具体障碍；`DONE` 全部完成条件及证据齐备。
“工程基础已有”不是 DONE。下表是基线状态，不是虚构的完成百分比。

| ID／工作包 | 基线状态 | 剩余缺口 | 依赖 | 完成证据 |
| --- | --- | --- | --- | --- |
| WP-01 候选到研究入口 | **DONE（G1）** | 有界发现、最新规则／观察时刻、一次完整预览和待审规格已接通；本项不包含模型授权或来源真伪认证 | 现有公开输入、PR #19／#20 门禁 | [PR #28](https://github.com/wmqfl861/polymarket-alpha-lab/pull/28)：52 项反例测试、三条 CI、真实 BTC／ETH 待审样例；详见第 10 节 |
| WP-02 真实模型与预算 | **AWAITING_OWNER** | 已实现同库共享调用额度预留和惰性客户端接线；仍需提供商／实际收费上界验证／数据授权／预算决定及真实研究收录 | D1—D3；可先离线开发，不需要先拿密钥；真实验收依赖 WP-01 | G2：两团队分别至少一次获准真实研究；原失败保留；重复请求不再次调用模型；授权与使用可核对 |
| WP-03 任务调度与恢复 | **PARTIAL** | 持久批次／公平轮转／共享额度已接统一受控入口：查询批次、轮次和预算，审核后显式入库批次／预算，显式运行一轮与停止恢复；独立脚本不装配真实客户端。仍缺获准提供商／收费上界验证及最终 G3 组合验收 | 复用现有领取／收录；先合成模型，真实运行依赖 WP-02／D3 | G3：两轮调度＋一次停机重启＋混合失败／中断场景，任务不丢失、不重复执行、不隐去 incomplete |
| WP-04 结算闭环 | **PARTIAL** | 已接原预测／候选哈希绑定的人工确认入口，核对原规则、声明来源与分钟时刻，并复用原子结算保存；仍缺真实 BTC／ETH 前瞻预测、实际匹配来源和独立人工核验 | WP-01 支持事件；WP-02 真实记录；事件到期和独立人工核验 | G4：BTC／ETH 各至少一个真实事件保留预测到结算的完整证据；过期、争议、拒绝和未决路径可追踪 |
| WP-05 研究到模拟评估 | **PARTIAL** | 已接同库模拟证据、审核结算损益下界、--settled-paper 评估及现有任务命令的保存／查询入口；全部尝试、未决和缺失仍保留。仍缺获准真实预测、真实输入／费率证据及完整操作验收；假设成本不等于账户收益，准入不保证提交确认先于截止 | WP-02、WP-04；订单簿和成本记录；可先做离线组合 | G5：两团队至少各一个完整工程样本；拒绝项、样本缺口、成本和时间均可复算，不据此声称策略有效 |
| WP-06 操作与发布收尾 | **PARTIAL** | 已有统一操作说明和入口源码隔离；已接整包预算到结算组合、提交后输出故障及旧冷备份目录前缀兼容；同版本整包冷恢复候选待最终验收；获准配置、安全数据版本切换和长期 Release 仍待办；PR #30 的 PS5.1 首跑超时根因仍未定位 | 文档整理现在可做；最终验收依赖 G1—G5 | G6：固定版本 Windows 全流程＋负向恢复验收＋发布资产哈希；无本机补丁拼装、无覆盖旧数据 |

**当前完整验收：1／6（仅 WP-01／G1）。** 这是六个端到端工作包的状态，不表示现有代码完成量为零。
每项达到 DONE 必须填入确切 PR、head／merge／tree、执行命令、结果、适用平台和未执行项；
真实业务记录在项目数据库私下留存，GitHub 只放已审阅的脱敏验收摘要。

## 5. 每个工作包的停止条件

### WP-01：先解决主流程，不新增旁路报告

- 只处理有界搜索结果；检查最新规则和实际取价时刻，保留失败、拒绝、未检查、无候选的区别。
- 明确请求总上限及每次重试原因。到截止时间停止下一步，不延长截止时间凑成功。
- 待审规格携带市场 ID、条件 ID、规则绑定、研究时间窗及来源用途；筛选不等于批准。
- 不支持的规则、源不匹配或已发生观察不得启动研究。最后启动前再次验证，避免继承旧资格。
- 完成 G1 后转 WP-02／WP-03；额外排行榜、报告或健康摘要不在这个工作包内。

### WP-02：有授权才有真实模型验收

- D1—D3 必须由用户明确决定。模型客户端的标签不证明实际提供商，编码 Agent 的登录不等于项目配置。
- 实现前先审阅凭据隔离与数据发送设计；实际凭据只在用户批准的本地运行边界提供，不进入聊天、GitHub、报告或研究证据。
- 新任务先记录前瞻性请求和领取，再执行模型，完整保存成功／阻断／失败；输入无效时不浪费付费调用。
- 同一任务重放返回原结果；不自动重试付费模型或借换 task ID 绕过 incomplete。恢复路径不抹掉首次失败。
- 调用次数、并发和请求大小在发送前受限；费用必须有可执行约束。响应后 token 统计不是硬金额封顶。
  无法落实用户要求的预算边界时，真实执行保持 BLOCKED，不以“尽力而为”冒充限额。

### WP-03：连续运行有明确边界

- 复用项目数据库、原任务身份和领取协议。队列运行元数据如需持久化，只能使用同一项目 PostgreSQL。
- 显式启动和停止；不默认注册系统服务、不无限重试、不从模型回答产生新任务。
- 先用合成模型验证正常轮次、部分失败、截止到期、进程中断、重启和并行重复请求；故障注入不使用付费模型。
- 不靠记录年龄猜 worker 已死并重新执行；丢失结果保留 incomplete，原评分完整性约束继续生效。
- G3 的场景验证说明可运维，不宣称无限期稳定、严格实时或跨机器分布式能力。

### WP-04：可人工核验，不假装自动最终性

- 复用既有候选采集、审核提交和原子结果保存接口；结算来源必须匹配合约指定的市场／时间／粒度。
- 人工取得实际来源证据是首版允许的流程。自动全历史采集、链上验证及未知类型解析推迟。
- 原预测须早于观察时刻；确认和未确认记录不可混同，后来结算不能改写旧候选。
- 争议、50/50、错误来源、时间冲突、缺失证据保持未决或阻断；不能默认 NO、自动覆盖结果或补造历史。
- 真实事件未到期或无法独立核验时记录 WAITING，不通过合成结算宣布 G4 完成。

### WP-05：软件闭环与策略有效性分开

- 先核对已存在组件，再做必要适配。模拟成交使用可执行 bid／ask、深度、手续费和可说明的滑点；不以 midpoint 代替可成交价。
- 实验名义数量是明确的模拟参数，不是资金建议或真实下单量。无真实订单、钱包或自动头寸配置。
- 保留全部研究尝试和模拟拒绝项；按一致时间、模型、协议版本和事件分组，展示样本分母、缺失结算及成本。
- 每团队至少一个真实工程样本仅证明接线可运行，不证明校准、收益或统计显著性；样本不足必须明示。
- 原 [Gate 0—8](docs/research/validation-gates.md) 不被此处替代。进入更高等级的
  200 次候选观察、50 次模拟交易、30 次已结算／退出交易，以及深度策略至少四周前向采集等
  既有起始门槛仍适用；V1 软件可交付不等于策略获晋级或实盘获授权。

### WP-06：固定版本交付，而不是继续累加功能

- 一份 quickstart 串起配置、发现／审阅、研究、任务管理、结算和评估；优先组合现有入口，不预设新 UI。
- 使用同一固定版本跑完整隔离 Windows 路径，验证停止／重启、错误配置、网络失败和无业务写入的只读检查。
- 正式本机验收只安排云端无法替代的步骤；不反复重跑已经通过且未受影响的数据库、网络或下载检查。
- 发布长期可达的明确标记研究／模拟用途的 Release：原始测试 ZIP、SHA256、源码 commit／tree、依赖和升级限制。
- 旧运行时及数据库保全；新源码可经审核指向原物理项目根。旧 kit 不覆盖解压，任何数据迁移另行设计与授权。
- 自审发现清零、最终差异与最终测试树一致后才给 V1 结论。既有未执行检查、平台限制、预算和数据限制随版本说明。

## 6. 实施顺序、并行与完成判据

关键路径：**WP-01 → WP-02 → WP-04 → WP-05 → WP-06**。
WP-03 可在现有领取／存储接口上先做离线设计和实现，但必须在最终交付前接入；
WP-02 的无凭据装配设计、WP-04 的人工操作流程、WP-06 的文档整理也可并行，写入范围不得冲突。
模型决定未到位不阻止这些安全的工程工作，不能反复把“找密钥”交给本地 Agent。

WP-01 已按第 10 节关闭 G1，不继续扩充筛选排行榜或旁路报告。
**下一实施重点：WP-02 获准客户端／收费约束与待决授权；其间推进 WP-04 结算衔接和 WP-05 离线组合。WP-03 受控入口已接通，真实模型组合验收仍待办。**
真实模型执行仍等待 D1—D3；可先用合成输入验证，不要求用户重复网络或数据库验收。
先核对允许的发布路径和已有代码；不得把历史未发布工作假定为现成依赖。
若写入被平台拒绝，记录具体障碍，不换接口、编码或本地代传绕过；可继续不依赖受阻内容的工作包。

每个 PR 开工前写清 `WP-ID + 要关闭的验收缺口 + 最小改动范围 + 所需授权`。
开发完成后由协调者作单独自审及反例验证，不宣称第三方／全新 Agent 审核；
相关测试和适用的最终 CI 必须通过，同步总表证据，再合并。
只修首版阻断性问题或本次改动引入的缺陷；其他增强登记在文末延期区，不自动扩大首版。
发现会改变范围或完成条件的需求时必须重新取得用户同意，不能悄悄降低门槛或增加第七个工作包。

**发布判据：G1—G6 全部有可复核证据，D1—D3 已明确且对应授权受遵守，
无未解决的首版阻断缺陷，同一发布版本通过最终集成与自审。**
未达到前只能报告“工作包推进／组件完成”，不能报告整个阶段完成。
到达后冻结 V1；统计样本积累和后续领域扩展使用新的里程碑，不自动变成本期永远做不完的尾项。

## 7. 用户待决事项与授权边界

| 决定 | 当前状态 | 需要的非敏感信息 | 影响 |
| --- | --- | --- | --- |
| D1 提供商和准确模型 | **未知，未选定** | 提供商、可用接口／模型标识、获准客户端工厂或装配方式 | 阻断真实模型验收，不阻断离线设计 |
| D2 研究输入发送许可 | **未授权／未知** | 是否允许把选定市场公开问题、完整规则和批准证据发给该提供商 | 没有明确许可不发送 |
| D3 首轮规模和费用约束 | **未知，未确定** | 事件数、每任务最大模型请求数、并发、总预算及运行频率 | 没有可执行预算不开始真实运行 |

不在聊天中索要 API 密钥，也不探测是否存在 `.env` 或用户凭据。用户同意此交付计划，
只冻结产品范围和开发优先级，不自动等于批准上述数据发送、凭据处理或业务执行。

## 8. 进度更新与计划可信度

上轮提出的工期是粗粒度资源估算，不是承诺日期、后台执行安排或已经验证的进度。
范围现在才冻结；在 WP-01 合并和 D1—D3 确认后，用实际工作量、阻断时间与可并行性重估。
不按测试数量、PR 数量或对话轮数换算完成百分比；工程人日与等待事件到期的日历时间分开。
长期十团队和实盘路线保留，但没有自动排进当前六包或获得实施授权。

以后每轮交付摘要采用同一口径：
**本次 WP／关闭了哪条 G 的子条件 → 固定合并版本与实测 → 工作包尚余缺口 →
六包状态是否改变 → 下一关键路径动作／真正需用户或本地执行的事项。**
没有真正本地必做项就不派发任务；有文件交接则遵守 GitHub 固定提交＋校验＋完整提示词规则。

## 9. 延期区与历史来源

延期：另外八团队、广泛路径依赖历史采集、自动最终性验证、新仪表盘、额外诊断叠层、
框架重写、分布式多机工作者、跨机器数据库迁移、实盘与自动资金配置。
严重缺陷不是延期增强，仍需在受影响功能启用前处理，但不把局部修复另命名为产品阶段。

参考历史，不作为当前交付状态：
- [2026-07-12 进度路线图](docs/roadmap/2026-07-12-project-progress-roadmap.md)
- [策略团队架构待办](docs/strategy-team-architecture-implementation-backlog.md)
- [自动化 Level 0—5 长期路线](docs/superpowers/specs/2026-06-13-automated-investment-roadmap.md)

本计划建立只涉及文档和项目规则，没有部署调度器、批准模型、登记市场、写入用户数据库、
运行真实研究或发布新应用包。表中空缺必须由后续实际工程和授权验收填满，而不是靠改状态文字。

## 10. WP-01／G1 验收记录（PR #28）

**G1 已关闭：候选到待审规格已接通并实际验收；不等于研究已经获批或模型已运行。**
本轮实现基于 PR #27 `4618fb51af4066be26b0f09cc7a328d4e9888828`。
运行代码提交 `daa523b17111ff55f8048fd69fb6c6b4581426c9`，代码树
`d74116b5a84075d30ff27e74ddadef45b2b41eca`。
其后只更新本总计划记录验收，运行代码、测试与工作流不变；最终 head／merge／tree 及
最终提交的重复 CI 结果固定记录在 [PR #28](https://github.com/wmqfl861/polymarket-alpha-lab/pull/28)。
合并仍须该最终提交适用的 CI 全部通过，不能用这里的前一提交测试代替失败的最终门禁。

### G1 对应能力和单独自审

- 在已有 `scripts/discover_crypto_research.py` 增加显式 `--select-supported`，不是新建旁路查询／报告命令。
- 一个搜索页、有界详情、既有合约／实际时刻门禁、首个合格项的一次重新取数预览及未批准的待审规格。
- 不延长 cutoff，不重试详情／预览，不尝试第二份完整预览；失败、拒绝、未检查、无候选分别保留。
- 自审两项反例先失败再修复：缺失开放状态不能当作完整负面证据；搜索尝试轨迹不得超过调用方预算。
- 新增 **52 项合成测试通过**，相关回归 **381 项通过**，另有 **648 组有限状态／顺序／计数对照**通过。
  这些是同一协调者的单独自审和有限验证，不是第三方审计或无缺陷保证。
- 本地完整验证 **37040 passed／31 skipped**；Python 3.13.5／pytest 9.0.2 使用容器现有依赖，
  不冒充 GitHub 锁定环境。日志 SHA256：`58bc767c3fcf6c6eea38003cff9cef300bbeb6535ea73a34639a52865b56c8a4`。

### 固定运行代码的仓库验收

| 验证 | 实测结果 | 固定 run |
| --- | --- | --- |
| 锁定依赖完整离线验证 | 37040 passed，31 skipped；安装／入口／编译及源码保全通过 | [34847301418](https://github.com/wmqfl861/polymarket-alpha-lab/actions/runs/34847301418) |
| Windows 原生数据库及相关回归 | 1300 passed，2 skipped；52 项新测试全部执行，无失败／错误 | [34847301431](https://github.com/wmqfl861/polymarket-alpha-lab/actions/runs/34847301431) |
| 实际 Windows kit 构建／解压／启动 | 96 passed，0 skipped；两个解压环境均通过新选项禁用模式 | [34847301533](https://github.com/wmqfl861/polymarket-alpha-lab/actions/runs/34847301533) |
| 真实公开候选到待审检查 | BTC、ETH 均 ready_for_operator_review；不调用模型／数据库，不批准研究 | [34847442073](https://github.com/wmqfl861/polymarket-alpha-lab/actions/runs/34847442073) |

31 个默认跳过项中，18 项 PowerShell 和 9 项原生检查在对应 Windows 流程执行；
4 项旧数据库 opt-in 本轮未执行。两个 Windows 跳过项为既有特权符号链接测试。测试选择有重叠，不能相加。
原生数据库和应用包使用隔离实例及合成业务输入；真实公开检查是另一项证据，未访问用户业务库。

### 真实 BTC／ETH 支持实例（2026-09-14 13:09 UTC）

在 Ubuntu 工程 runner 实际执行一次，未在外层重试（不是用户机器的运行）：

```bash
.venv/bin/python -I scripts/discover_crypto_research.py --team crypto_btc --team crypto_eth --select-supported --max-candidates 10 --attempts 3 --allow-public-fetch
```

| 团队 | 实际市场 slug | 初筛／完整预览 | 观察时刻与临时截止 |
| --- | --- | --- | --- |
| BTC | bitcoin-above-68k-on-september-14-2026 | 131 个搜索候选，检查 1 个、130 个未检查；一次完整预览，待人工审阅 | 观察 16:00 UTC；临时截止 13:39:18.874366 UTC |
| ETH | ethereum-above-2000-on-september-14-2026 | 122 个搜索候选，检查 1 个、121 个未检查；一次完整预览，待人工审阅 | 观察 16:00 UTC；临时截止 13:39:20.481215 UTC |

两次搜索均首次成功，未声称覆盖整页或全部市场。实际搜索 2 次＋详情 2 次＋两份完整预览的
GET 上界 6 次，合计上界 **10／配置上限 32**；`eligibility_scan_complete=false`、
`selection_is_approval=false`、`operator_approved=false`、模型／数据库调用均为 false。

两个合约都是 Binance BTC/USDT 或 ETH/USDT 的 ET 正午一分钟 Close；参考行情是 Coinbase／Kraken
USD 小时线，窗口 09:00—12:00 UTC、3 根。来源／交易对／粒度差异已在结果中显式标注，
`source_suitability_review_required=true`、`settlement_source_verified=false`。G1 只证明可进入待审路径，
不能将其解释为来源已认证、人工已批准、真实模型已运行或结算已确认。

公开证明 artifact `10348885551` 已下载并验证；ZIP SHA256
`32614b0a3fc25962dd0d0936a7a35834f57fb4a86c178047767bf07f3f7222af`；
17689 字节日志 SHA256 `a4d2fee6aa2780f657cc20c81289ca4ba815875d8eb2ab508778dc33084b4745`。
固定证据摘要及完整条件 ID／来源哈希保留于 PR #28 的评论 `5664592502`，不依赖会过期的 artifact 才能知道结论。
原始公开响应只在内存处理，不新增文件业务历史；上述临时 cutoff 和 hash 不得复用为后续授权。

### 后续边界

WP-01 DONE 不改变 D1—D3 或其他五包状态；不降低 G2—G6。下一步是模型装配／预算的无凭据设计和
有界任务运行接线，实际模型及业务写入仍待单独授权。本轮无新迁移、依赖升级、长期 Release 或用户安装覆盖。
工程工作流用于固定代码公开验证，不进入应用运行时。没有新的本机必做项，不安排重复的本地 Agent 验收。


## 11. WP-03 持久批次与有界执行（PR #29；G3 尚未关闭）

基于 `cd74397ad87547c143dd6b926a6dc4554c34e0d4`（WP-01 已完成），复用现有任务领取、
收录和项目数据库会话，增加 [持久批次运行](docs/research-dispatch.md)。
本项是执行主流程接线，不是新增旁路报告／仪表盘，也不冒充常驻定时服务。

- 一次显式入队保存完整有界 BTC／ETH 请求；同 ID 原样重试返回原时间，改内容拒绝。
- 显式运行只启动仍新鲜且无领取记录的任务；捕获失败和 incomplete 原样保留，不按年龄重跑。
- 一轮任务／并发数有界，停止阻止后续入场，已入场工作自然结束；按原批次 ID 在新进程读回。
- 新增第 64 份迁移 `20260914000000_research_dispatch_batches.sql`；原 63 份字节不变。
  仅在隔离实例验收 63→64 和历史保全，未迁移用户实例；旧 kit 不覆盖、不自动升级。
- 实测命令：`python -m pytest -q tests/test_research_dispatch.py`；原生另显式启用
  `tests/test_project_postgres_dispatch_native.py`；完整 `scripts/verify_local.py --full`
  和适用 Windows／分发 CI 的最终固定版本证据记录在本变更 PR，失败不替换为旧版本结果。

**PR #29 时 WP-03 仍 PARTIAL；V1 仍 1／6。** 当时单批次有序限量并非跨批公平调度：
无领取的持续数据库／身份错误可能消耗下一轮额度；没有耐久错误轮转、统一启停操作入口
或共享提供商费用约束。已领取任务绝不因此重跑；这些缺口及 WP-02 预算联动完成后再判 G3。
D1—D3 未变，本变更不选择模型、不读密钥、不发送真实研究、不写用户业务数据库。


## 12. WP-03 本次推进：持久公平轮转（G3 尚未关闭）

基线：`122b0976fd7a0b10e3d3bc67a2aba4eab75f551d`（PR #29）。
本项只关闭 G3 的固定名册轮转／失败任务让位子条件，不新增第七工作包。

- 复用原批次和执行器，将 1—10 个已入队批次按位置交错，合计最多 100 个请求。
  每轮上限仍显式指定；游标在调用模型前提交到同一项目 PostgreSQL。
- 前面任务的领取前错误不永久占住下一轮额度。新轮次从持久游标继续；已经领取但无结果
  的任务仍为 incomplete，不按年龄重跑，严格评估仍阻断。
- 同轮次编号重放只返回原选择凭据，零模型执行；不把选择凭据当成已执行／已完成证明。
  停止、提交不确定、进程中断和批次读取失败分别处理，不宣称自动恢复丢失的结果。
- 新增第 65 份迁移 `20260915000000_research_dispatch_turns.sql`，原 64 份字节保全。
  仅隔离源代码实例执行 64→65 验证，旧批次和记录保持不变；不升级用户实例或覆盖旧 kit。
- 自审按固定源码核对身份／请求哈希、环形顺序、停止、幂等、并发和失败保留。
  专项命令：`python -m pytest -q tests/test_research_dispatch_rotation.py`；
  原生证明：`tests/test_project_postgres_rotation_native.py`（显式 opt-in）。
  最终提交、全量验证、Windows 和分发结果及首败记录固定在本变更 PR，不借用前一提交结果。

**六包状态仍 1／6；WP-03 PARTIAL。** 新轮转仅覆盖明确的不可变名册，不是全库自动调度、
常驻日历服务或全局预算。批次无法完整读取时整轮拒绝，不能靠跳过坏数据凑成功。
统一操作入口、获准模型／费用联动和最终 G3 组合验收仍未完成；D1—D3 未变。
后续应接 WP-02／WP-03 的受控操作路径，不再增加旁路诊断或重写研究框架。


## 13. WP-02／WP-03 本次推进：共享模型调用预算预留（G2／G3 尚未关闭）

基线 `16b7594964d424cce51c7e7afb20ed7eecbbe054`（PR #30）。本项将
[同库调用额度预留](docs/research-model-budget.md)接入原单任务／批次／轮转执行路径，
不是新增旁路诊断、提供商选择或密钥读取。

- 显式不可变预算限定具体请求哈希、模型标签、过期时刻、调用数、消息字节和输出 token；
  以整数货币微单位保守预留整次费用上界，不把响应后的用量统计当成发送前限额。
- 每次实际客户端入口前提交额度；多个工作者共享同一额度，重放凭据不再次执行；
  超时、错误、确认丢失或进程中断都不自动退额。预算已空／已过期不领取新任务。
- 只在已有任务领取后使用惰性客户端；未完成记录、失败收录和原严格评估规则保留。
  预算中途耗尽可留下原有 `model_failed` 结果；原 `model_calls` 不是实际发出的收费请求计数。
- 新增第 66 份迁移；原 65 份不改。仅隔离源代码实例验证 65→66、并发上限、断进程后
  额度不返还及旧记录保全。旧 63→64、64→65 验证仍针对各自原目标，不升级用户实例。
- 专项 `tests/test_research_model_budget.py` 和显式原生
  `tests/test_project_postgres_budget_native.py`，以及完整离线／Windows／分发验收的
  最终提交和首败记录固定在本变更 PR；未执行的检查不能用模拟结果代替。

**V1 仍 1／6，WP-02 AWAITING_OWNER，WP-03 PARTIAL。** 这是可执行的内部预留额度，
不是提供商账单上限认证：真实适配器须保证每次调用不隐含重试或额外操作，收费上界必须
覆盖所有费用并得到独立验证。当前没有选定／验证真实提供商或费率；`cost_bound_attested`
和参考哈希是调用方声明，不是身份或价格真伪认证。D1—D3 未明确前不运行真实研究。
WP-03 的统一操作入口、实际获准模型／计费联动及最终组合验收继续待办。

WP-06 继续保留 PR #30 的 Windows PowerShell 5.1 首次启动／管道超时问题：
同提交第二次通过不等于根因消失，不允许预热、扩大超时或静默重试到绿来关闭最终验收。


## 14. WP-03 本次推进：统一受控任务入口（G3 尚未关闭）

基线 `038be0c0df91999e6038e8fefe2830022b2780fb`（PR #31）。
复用[批次／轮转操作说明](docs/research-dispatch.md)及现有托管会话，增加
`scripts/manage_research_tasks.py` 和 `research_dispatch_cli.main`；不是另一套队列、
报表、调度服务或凭据配置器。查询原批次／轮次／预算，显式运行一轮必须同时给出
已创建预算、模型调用许可和应用提供的获准客户端工厂。独立脚本没有客户端配置，
拒绝实际运行；不把无凭据的接线冒充真实提供商接入完成。

- 同轮次重放不执行；新轮次从持久游标继续。停止后未入场项仍在原批次，
  已领取但无结果项保留 incomplete，不按年龄、换编号或换预算重跑。
- 只有托管会话清理成功后才输出原元数据回执。失败、捕获失败、缺失结果和中断
  不被“dispatched”或退出码 0 混同为全部完成；异常后明确保留可能已调用／已写入。
- 不输出原始输入、模型内容或 pending_run；需要保留原结果作 capture-only 恢复时
  使用既有类型化会话 API，不能从控制台回执重建预测。原严格评估門槛不变。
- 无迁移或依赖变更；新脚本纳入新构建包的可选入口，不改旧包清单，不覆盖用户安装。
- 同一协调者单独自审先复现六项 SystemExit 绕过失败状态的反例，再修复异常边界；
  保留首败而不削弱断言。专项命令：`python -m pytest -q tests/test_research_dispatch_cli.py tests/test_research_dispatch_cli_review.py`。
  原生组合证明：`tests/test_project_postgres_dispatch_cli_native.py`（显式 opt-in），
  覆盖两团队多轮、停止重启、失败留存、真实子进程中断和额度不返还；输入／客户端仍为合成。
- 精确最终 head／tree、独立工作副本自审、全量离线及 Windows／分发 CI 结果和未执行项
  固定记录在本变更 PR；不得用旧版本通过记录代替当前最终门禁，也不宣称第三方审核。

**WP-03 仍 PARTIAL，WP-02 AWAITING_OWNER，V1 仍 1／6。** 本项只关闭受控操作入口
子任务；D1—D3、真实适配器与收费上界核验、最终获准模型组合验收仍待完成。
WP-06 的 PR #30 PowerShell 5.1 首跑超时根因仍未关闭。没有真实模型调用、用户业务库
读取／迁移、密钥发现、长期 Release 或实盘操作；不派发重复的本地验收。


## 15. WP-04 本次推进：原预测绑定的人工结算确认（G4 尚未关闭）

基线 `62a6337d0484d3dd2cc457a6b2d468282a33204e`（PR #32）。
复用[结算清单命令](docs/research-resolution-queue.md)、原始执行／候选查询、既有合约／观察时刻门禁
和原子结算保存接口，增加 `--confirm --allow-resolution-write`；仅显式读取一份有界的
人工已核验 stdin 输入，不新建文件业务队列、结算表、网络采集或评分系统。

- 绑定原完成预测的记录／请求哈希及原未确认候选的编号／哈希；新旧问题／规则必须逐字一致。
  已保存失败、缺失结果、规则改动、来源描述或时刻冲突均不晋升为已结算结果。
- 声明 Binance BTCUSDT／ETHUSDT、1m、Close 必须匹配受支持原规则；原预测先于 cutoff，
  cutoff 先于实际分钟开盘；结算不能先于收盘，人工确认不能先于候选保存。
- 来源标签、引用网址和人工布尔值不是来源真伪认证；实际 Close 和 YES／NO 仍须人工独立核验。
  原证据文本及独立哈希连同记录／候选绑定保存在既有 confirmation.source_text 的版本化封套内。
- 新审核与 outcome 用既有事务原子保存；原预测和旧候选不改。同输入同版本重放保留原时间，
  不自动重取候选／重试／覆盖结果；异常可能已提交。拒绝的 stdin 不新增耐久错误日志。
- 同一协调者单独反例自审先复现三项“同审核编号、不同原预测／结果／来源仍显示成功”的反例，
  再补控制台回执完整绑定，不削弱断言。原生测试设计还纠正了在父会话持锁时启动子进程的问题，
  调整测试顺序而不放松生产生命周期锁。没有宣称第三方／全新 Agent 审核。
- 专项：`tests/test_research_resolution_confirmation.py` 与 `tests/test_research_resolution_confirmation_review.py`。
  原生组合：`tests/test_project_postgres_confirmation_native.py`，使用真实数据库时钟、前瞻性合成预测，
  等声明分钟实际收盘后走真实控制台、保存 YES／NO、重放重启、旧记录保全及原评估器。
  精确最终 head／tree、全量离线／Windows／分发 CI、首败和未执行项固定在本变更 PR。

**WP-04 仍 PARTIAL，G4 未关闭；V1 仍 1／6。** 真实模型／真实事件证据与独立人工核验
不能由合成测试代替。D1—D3 和 WP-06 的 PowerShell 5.1 首跑可靠性问题仍未解决。
无迁移、依赖升级、用户业务库访问、实际模型／行情调用、旧 kit 覆盖或实盘操作。


## 16. WP-05 本次推进：研究到成本／订单簿情景组合（G5 尚未关闭）

基线 `4a6e787e1b0a6fb93c0d36455987c66f0b043e7c`（PR #33）。
[研究到模拟情景](docs/research-paper.md)通过 `evaluate_paper_research` 复用原完整历史评估器、
首次尝试选择、成本风险策略和订单簿逐档成交模拟，不新增预测评分或业务文件持久化。

- 原全部研究决策逐项保留：失败、后续尝试、缺失输入和未决不从分母中消失；
  可见 incomplete 仍先被原完整性门禁阻断。原记录哈希和不可变领取／结果必须精确匹配。
- 仅受支持 BTC／ETH 固定时点规则；复用合约与实际观察时刻门禁。市场状态、最小数量、
  价格步长、Yes／No token 映射、双边订单簿和采集时间均显式检查，不靠缺省值放行。
- 逐档模拟完整请求数量；部分深度保留但不自动缩量或计作交易。成本输入使用最差已消耗 ask，
  不用 midpoint 或显示舍入后的均价计算金额；费用和风险参数必须显式提供。
- 在声明的二次每股手续费情景内，对消耗价格范围取费用上界；费用向上、预期净优势向下舍入。
  低于原阈值时拒绝，不用舍入凑成功，也不改原概率或重选另一方向。不是实际费率／账单认证。
- 单独反例自审复现并修复阈值舍入、费用上界、观察准入、最小数量和坏 JSON 处理五类遗漏。
  首跑一个舍入反例本身使用了不合法精度，修正测试输入后在未修实现上重现；两份首败分开保留。
- 专项：`tests/test_research_paper.py`、`tests/test_research_paper_review.py`；原生组合为
  `tests/test_project_postgres_paper_native.py`。独立 Windows 流程验证只读组合、重启／原记录保全和
  incomplete 阻断；原长流程测试及超时不变。最终提交、实际 CI、原始日志和未执行项固定在本 PR。

**WP-05 仍 PARTIAL，G5 未关闭，V1 仍 1／6。** 本次仅回顾性内存情景，不是前瞻模拟交易证据；
`paper_trades_created=0`、`durable_paper_evidence_created=false`、`realized_pnl=null`。
前瞻输入／拒绝项／模拟执行的同库耐久记录、实际结算关联和真实工程样本仍待完成。
不以历史时间戳或哈希冒充来源／前瞻性认证；原统计晋级门槛、D1—D3 和 WP-06 已知问题不变。
无新迁移、依赖升级、真实模型／公开行情调用、用户数据库访问、旧 kit 覆盖或实盘操作。


## 17. WP-05 本次推进：前瞻模拟证据同库原子保存（G5 尚未关闭）

基线 `0147b5777be00296f9e981b956275b7b11b8e7f9`（PR #36）。
`capture_paper_research`／`inspect_paper_research` 复用原完整历史检查和模拟算法，
同一私有 PostgreSQL 按原研究记录保存唯一的规范输入及结果；原只读情景 API 不变。

- 原始 Gamma／YES／NO 字节、成本／数量／时刻及接受、拒绝或未模拟结果原子保存；
  失败和后续尝试不被另选成成功样本，原研究／结算历史不改。绑定不合法或未提交的错误不伪造保存。
- 数据库实际时间检查原截止和新鲜度，触发器覆盖调用方插入时间并在提交前再检截止；
  这是前瞻准入证据，不保证 WAL 写入或提交确认早于截止，不认证远程来源时间。
- 同原记录只保留一份输入；原样重放保留首次时间，换参数冲突。提交确认丢失需显式查询／原样重放，
  无自动重试、换编号或改写拒绝。查询只重算单条完整性，不冒充当前全库历史检查。
- 新增第 67 份迁移，原 66 份字节不变；隔离实例验证 66→67 和旧记录保全。
  旧 65→66 预算测试固定原目标；现行完整目录回归断言明确更新为 67，不隐藏尾迁移。
- 单独自审先复现三个 INSERT 回执的来源绑定遗漏，再补齐一致性检查；首败保留、断言不削弱。
  原生证明覆盖两团队、拒绝／失败、并发重放、实际提交后确认丢失、重启、禁止修改和延期截止回滚。
  固定提交／代码树、最终全量与 Windows／构建 CI、源码校验和未执行项记录在本变更 PR。

**WP-05 仍 PARTIAL，G5 未关闭，V1 仍 1／6。** 同库模拟证据不等于真实成交、投资组合账本或
已实现损益；真实输入／费率验证和结算损益关联仍待办。D1—D3、统计晋级门槛和 WP-06 已知问题不变。
没有真实模型／行情调用、用户业务库访问、凭据发现、旧 kit 覆盖或实盘操作。


## 18. WP-05 本次推进：保存的模拟证据关联审核结算（G5 尚未关闭）

基线 `a257a7f921e412e348d2a17efd2b53c4f7530918`（PR #37）。
[结算后的模拟损益](docs/research-paper-settlement.md)通过 `evaluate_settled_paper_research`
在同一只读快照内复用原完整历史选择、不可变模拟回执及原 crypto 人工确认流程。

- 原研究查询体等价提取为游标 helper，原校验／选择算法不改；新入口同一事务读取历史、
  模拟、原预测／候选及确认，避免晚提交记录混入旧快照。可见 incomplete 仍整体阻断。
- 用既有确认构造器重建并比较完整来源绑定；相同条件／规则才关联。旧直接 outcome
  或不满足 crypto 确认流程的结果继续可见，但不给损益；损坏的声明证据不被静默跳过。
- 结算给付减去原已保存的成本上界，得到模拟损益下界；不事后重选方向、重估成本或
  把原拒绝变成交易。未决／缺失没有虚构的零收益，不混合不同模型、协议或成本假设。
- 无新迁移、费用推断、业务写入、账户操作或平行评分；67 份原迁移保持不变。
  `actual_account_pnl=null`、`settled_subset_only=true`，不是实际收益或策略有效性证明。
- 单独自审复现错误但有效审核回执可被关联的反例，补齐 outcome 一致性检查；原断言
  保留。另一个首跑测试构造问题单独纠正，不将它冒充产品缺陷；首败日志分别保留。
- 原生测试使用合成 BTC／ETH 前瞻预测、实际分钟收盘和原确认 API，验证四种给付、
  晚提交快照隔离、旧记录保全、历史截止／重启及 incomplete 阻断。固定最终版本、
  实际全量／Windows／kit 结果、自审记录和未执行项保留于本变更 PR。

**WP-05 仍 PARTIAL，G5 未关闭，V1 仍 1／6。** 真实模型／输入／费率及完整业务验收仍待办；
D1—D3、原统计晋级门槛和 WP-06 首次启动可靠性问题不变。没有用户业务库、密钥、
真实行情／付费模型调用、旧安装覆盖或实盘操作；不派发重复本机验收。


## 19. WP-05 本次推进：已有评估命令接入审核结算视图（G5 尚未关闭）

基线 `f0ce4fa2be7d32874d88713a4c2248162b412d9d`（PR #38）。在既有
`scripts/evaluate_project_research.py` 增加 `--settled-paper`，直接展示原单快照结算
评估结果，不新增评分／报表系统、输入采集、数据库写入或另一套损益算法。

- 默认概率评估路径不变；新模式保留全部状态计数和成本策略分组，默认省略逐条数组，
  `--include-decisions` 才显示原模拟尝试及历史决策。未决／缺失仍为空值，不伪造零收益。
- 沿用历史截止、数量和诊断参数；回执与请求不匹配或相互矛盾时拒绝，不降级为旧评估。
  只在托管会话清理后序列化成功输出，异常／中断和输出流失败不转为退出码零。
- 单独反例自审先复现六项时间／数量／诊断参数和嵌套只读标记遗漏，再修复；原反例不削弱。
  原生组合通过实际命令子进程比对原 API；两个实际解压 kit 各用自己的环境运行新选项。
- 无迁移、依赖、权限、原历史选择／来源验证／金额算法或既有 CI 超时变更。
  最终代码树、相关回归、完整离线及 Windows／kit 实测、首败与未执行项固定在本变更 PR。

**WP-05 仍 PARTIAL，G5 未关闭，V1 仍 1／6。** 此项关闭结算视图的命令行接入缺口；
真实预测／输入／费率及完整业务验收不能由合成测试替代。D1—D3 和 WP-06 已知可靠性问题不变。
没有真实模型／行情调用、用户业务库访问、旧 kit 覆盖、凭据处理或实盘操作。


## 20. WP-05／WP-06 本次推进：任务命令接入模拟证据（G5／G6 尚未关闭）

基线 `747faeaa8a5d60776261d80941e061b80af0f9b4`（PR #39）。在既有
`scripts/manage_research_tasks.py` 增加 `capture-paper`／`inspect-paper`，复用原
模拟证据 codec 和托管保存／查询，不新建存储、评分、采集器或模型配置。

- 保存必须给原记录编号、已审阅规范输入的 SHA256 和明确写入许可；有界二进制 stdin
  只允许规范载荷及一个可选行结束符；额外内容和哈希不一致在数据库访问前拒绝。
- 查询不读 stdin、不写业务记录；保存回执验证原编号及完整输入一致性。
  返回原已保存拒绝仍是操作成功，不是交易批准；错误后保留可能已经提交的状态。
- 托管清理完成后才序列化输出；短写、输出／刷新失败和中断返回非零，不自动重试。
  旧批次／轮次／额度流程不变，已结算评估沿用 PR #39 命令，不重复实现损益。
- 单独自审复现辅助接口无效操作名回显的问题并修复；命令行解析本身原已拦截，
  不将接口边界遗漏夸大为数据库漏洞。首败日志和原失败断言保留。
- 独立阶段自审与真实 Windows 子进程保存／重放／冲突／拒绝／重启查询、固定最终
  代码树的四条 CI 及未执行项记录在本 PR。原 67 份迁移、依赖、存储和模拟算法不变。

**WP-05／WP-06 仍 PARTIAL，G5／G6 未关闭，V1 仍 1／6。** 输入准备仍须获准应用和
人工审阅，哈希不是来源／人工批准认证。真实输入／费率、D1—D3、完整操作验收和旧 PS5.1
可靠性问题仍待办。无用户业务库、密钥、真实模型／行情、旧安装覆盖或实盘操作。


## 21. WP-06 本次推进：PS5.1 首次用例的分段失败证据（G6 尚未关闭）

基线 `f331d71d76d116aabee3661e73a9621d52e3a1d6`（PR #40）。
[首次调用取证](docs/handoff-first-run.md)只改既有合成测试的观测与 CI，不修改生产下载脚本。

- 原 30 秒超时、命令参数、哈希／部分文件保全断言和 18 项 Windows 用例保留；
  stderr 固定阶段与耗时进入测试日志／JUnit，无预热、重试、模块路径或安全设置变更。
- 三个预先声明的独立 runner 分别首个执行 PS5.1 成功用例；不是失败后的重复尝试，
  也不声称 runner 启动期间没有运行过 PowerShell。要求的 shell 缺失时失败，不跳过到绿。
- 单独自审复现诊断写入错误掩盖原超时的两项反例，再修复；原异常和首败保留。
  阶段缺失不推断根因，最终输出标记也不把非零退出或超时变成通过。
- 固定最终 head／tree、三个首调用结果、原生回归、完整离线验证及未执行项记录在本 PR。

**WP-06 仍 PARTIAL，G6 未关闭，V1 仍 1／6。** 这是遗留可靠性问题的取证子任务，
不是根因修复；三次成功也不能关闭原间歇故障。D1—D3、真实输入／费率和业务验收不变。
没有用户机器／业务库操作、真实下载、模型调用、凭据处理、旧 kit 覆盖或实盘操作。


## 22. WP-06 本次推进：统一操作流程与入口版本隔离（G6 尚未关闭）

基线 `0c438098bab4d7c0a4f626664af43a31d96d8b08`（PR #42）。扩充既有
[quickstart](database/quickstart.md)，串联安装／发现、授权、预算任务、模拟保存、人工结算、
评估和停止重启；不新增操作框架、业务存储、评分或默认模型客户端。

- 复现数据库管理和结算清单脚本误用其他 editable 安装源码的反例，改为优先使用自身
  相邻源码；源码缺失明确拒绝，不借另一个安装继续执行。`--root` 仍只选择数据／运行时
  及迁移目录，不变成代码选择或旧 kit 升级接口；原参数、存储和权限边界不变。
- 分开自审复现缺失包回退问题并补齐拒绝；指南链接检查发现结算说明未随 kit 打包，
  加入可选文档清单，旧包无该文档仍可验证。首败和修复记录保留，不削弱原断言。
- 指南逐条示例交给实际参数解析器验证但不执行写入／网络动作；两个实际解压环境
  使用各自 Python 和源码验证入口不受另一安装路径影响，不访问用户数据。
- 最终版本的全量离线、Windows 原生／模拟／kit 验收、独立工作副本自审及确切代码树
  记录在本 PR。原 67 份迁移、依赖、研究／预算／模拟／结算算法和旧超时不变。

**WP-06 仍 PARTIAL，G6 未关闭，V1 仍 1／6。** 这是统一指南与源码选择修复，不是
通用旧库迁移／回滚、模型配置、旧 PS5.1 根因修复或正式 Release。D1—D3、真实输入／
费率和完整业务验收仍待办；不访问用户机器／业务库、不找密钥、不覆盖旧 kit。


## 23. WP-06 本次推进：结算命令异常与输出失败不冒充成功（G6 尚未关闭）

基线 `1ac3ec6b1ec0de18faa1e0227076b14075525ec7`（PR #43）。修复原
结算清单／候选采集及人工确认命令的退出、输入和输出边界，不新增业务或诊断系统。

- 清单／采集操作和会话清理中的 SystemExit 不再以零退出掩盖失败；中断返回 130。
  失败回执区分采集可能已发生网络请求／证据写入与纯清单读取，不从异常推断提交不存在。
- 两种路径复用现有确认模块中的输出函数：完整序列化后单次写入并显式刷新，短写、
  序列化／写入／刷新错误返回非零；不向已损坏流追加第二份回执，不自动重试业务操作。
- 单独自审复现默认 stdin.buffer 获取在确认输入保护范围之外的问题，移入原有输入
  检查边界。未获写入许可不读输入；已提交后的输出失败不撤销证据，不补造成功结果。
- 成功 JSON、失败数量退出语义、原权限／数量限制、来源绑定和存储／结算算法不变。
  60 项专项包含真实子进程的异常零退出和短写反例；首败、独立工作副本自审、最终
  全量及 Windows／kit 证据和未执行项固定在本变更 PR。无迁移或依赖升级。

**WP-06 仍 PARTIAL，G6 未关闭，V1 仍 1／6。** 本项不保证消费者收到输出，不恢复
丢失确认、不修复原 PS5.1 间歇故障，也不关闭 D1—D3 或真实模型／输入／费率验收。
命令行参数错误仍沿用原解析器；没有用户机器／业务库、凭据、真实行情／模型或实盘操作。


## 24. WP-06 本次推进：安装包内预算到结算的组合验收（G6 尚未关闭）

基线 `1ac3ec6b1ec0de18faa1e0227076b14075525ec7`（PR #43）。在既有实际安装包测试的
第二个全新解压环境中，使用其自己的 Python／项目源码串联原预算任务、模拟保存、人工
确认和损益评估；合成测试脚本不作为新应用、模型适配器或业务文件队列发布。

- 两个有界轮次之间实际停库重启；同轮次重放不新增调用，七次额度覆盖三次完成研究和
  一次失败。模拟接受、拒绝、研究失败均由原任务命令保存，原编号和输入不可覆盖。
- 等声明的实际 UTC 分钟结束后，走原确认命令和结算评估命令；核对 BTC／ETH 模拟金额、
  完整分母、历史视图和原 API 一致性，不把假设成本与合成结算冒充真实收益。
- 另一个真实子进程在领取和额度预留提交后退出；重启仍为 incomplete，额度不退还，
  原样重放不构造客户端；当前严格评估阻断，而中断前历史视图保持原样。
- 独立阶段自审、精确最终版本的完整离线／Windows／构建结果及原始日志记录在本 PR。
  既有组件断言、67 份迁移、运行算法、依赖、旧进程／CI 超时和权限保持不变。

**WP-06 仍 PARTIAL，G6 未关闭，V1 仍 1／6。** 这是安装包内组合工程证据，不是实际模型、
真实来源／费率或人工核验，亦非旧库升级、PS5.1 根因修复或 Release。D1—D3 和真实业务
验收仍待办；不访问用户机器／业务库、不读取密钥、不覆盖旧安装或提交真实订单。

### PR #44 合并衔接与真实提交后输出故障证明

整合基线为 `fff21287c0e3bfab2d679d7ac47bd8044f808ea9`（PR #45）；保留上面的
结算命令修复，原 PR #44 整包场景的旧测试结果不替代整合版本验收。原两分支提交不重写。
新增一个实际安装包内的人工确认输出短写：先查无记录，真实提交后的成功回执写不完整而
非零退出，再按原编号查询并明确原样重放；原回执与时间戳必须不变。只注入输出故障，
不模拟数据库／事务，不新造审核编号或重试模型。最终复审和适用 CI 以本轮固定树为准。
本项仍为合成工程验收，不关闭 G2—G6、D1—D3、真实输入／费率或原 PS5.1 问题。


## 25. WP-06 本次推进：旧冷备份的显式迁移目录前缀兼容（G6 尚未关闭）

基线 `4df08873f4b9b8f1378913c0d00ca6bb183e51e2`（PR #44）。复用原冷备份格式、
校验、恢复和迁移入口，只为已审阅 SOURCE 安装增加 `--allow-catalog-extension`。
默认仍要求完整目录一致；没有通用升级器、旧 kit 覆盖、业务文件存储或新数据库后端。

- 显式许可只接受旧备份指纹是当前完整已校验迁移目录的非空精确前缀。旧文件删除、
  改名、改写、排序变化、非法新增尾部、较新快照配较短目录均拒绝；根路径、引擎、
  信任／校验值和目标必须不存在的要求不变。恢复不启动数据库，也不执行迁移 SQL。
- 新回执明确目录数量不等于实际数据库账本；研究会话仍检查实际账本并阻断待迁移。
  独立自审复现“恢复期间尾部被改写但数量不变”仍通过的问题，补完整目录指纹比对；
  首败保留、原反例断言不削弱，失败只保留私有 staging，不覆盖原数据。
- 隔离原生证明以真实 66 迁移备份配当前 67 迁移目录，验证默认拒绝、显式校验、
  原位缺失目标恢复、实际待迁移阻断、显式一次迁移和成功／失败研究记录保全。
  固定最终树、单独工作副本复审、完整离线和适用 Windows／kit 结果记录在本 PR。
- 原 67 份迁移、依赖、权限、业务算法和备份格式不变；仅新增许可路径，不把恢复
  成功冒充来源真实性、当前账本已检查、消费者已收到输出或完整灾备／升级验收。

**WP-06 仍 PARTIAL，G6 未关闭，V1 仍 1／6。** 真实模型／输入／费率与 D1—D3、
具体版本的安全数据切换和原 PS5.1 间歇问题仍待办。不操作用户机器或业务库，不取密钥，
不上传含凭据的备份，不覆盖旧 kit，不运行真实模型／行情或实盘订单。


### PR #48 原生 CI 超时处置（不改变备份功能或验收门槛）

首次 head `e1e1df52314b4570bd75635fd1d173823f83e29d` 的原生 run
`35091514300` 因整个 job 达到 20 分钟取消；新 66→67 用例输出通过标记，
但没有最终 JUnit 或源码保全检查，整轮仍未通过，不以局部结果替代。

日志中的第二次 120 秒转储位于等待实际结算分钟的主动 sleep，不是已证实的
卡死；第一次位于 ACL 子进程通信，只能定位当时的调用栈，不能据此断定根因
与 PR #30 相同。本轮不修改安全设置、真实分钟等待、进程时限或产品代码。

将原 51 个测试文件原样、无遗漏且不重复地分配到 storage/research/dispatch
三个独立 Windows job；每个仍限 20 分钟，无自动重试，fail-fast=false。
总可用 runner 预算明确增加为最多三个并行 job，不再要求全套串行挤入一次
20 分钟。原 windows-native 名称保留为汇总检查，任何失败、取消或跳过均拒绝。
每组保留原环境、锁定依赖、诊断时限、全部用例及源码保全检查，单独保存日志
和 JUnit；追加耗时显示及测试清单／汇总状态反例检查。最终版本必须三组全过，
并与原清单的实际收集 ID 多重集合核对后才可验收；首次取消证据永久单列。

这是 CI 调度修复，不是底层 PS5.1 间歇问题的根因修复或完整发布验收；
WP-06/G6、D1—D3、真实来源／费率及用户数据升级限制不变。


## 26. WP-03／WP-06 候选：任务命令完整输出与共享停止（待 Windows 验收）

基线 `3a987bc6c1a5df608ff8f0668b866aaf87842c04`（PR #48）。本项独立于仍为
草稿的 PR #49；不携带其安装包检查改动，也不重启被拦截的 CAPI2 诊断试验。

- 任务清单／轮次／预算查询与预算轮次执行复用已有受控输出函数，完整序列化后一次写入
  并核对字符数和刷新。短写、写入／刷新／序列化异常返回非零，不补发第二份输出或重跑业务。
- 单独自审复现输出阶段中断丢失共享停止信号的问题并修复；收到该中断时返回 130 并请求
  原停止令牌，普通输出失败不停止其他关联运行。已准入任务仍自然收尾，不推断数据库回滚。
- 正常 JSON、原状态与费用预留／任务身份／权限／输入校验保持不变；不新增输出模块、
  模型客户端、持久化或工作流。四个真实子进程反例使用合成会话，不冒充数据库原生验证。
- 已有原生场景新增真实空轮次提交后输出短写、原编号查询与明确原样重放；应证明回执和
  预算额度保持不变。该场景仅在 CI 私有测试实例执行，最终提交的全量／Windows／kit 门禁
  和原始首败证据须齐备后才能验收，不以本地测试代替。

**本项仍待发布与原生验收，WP-03／WP-06 PARTIAL，G3／G6 未关闭，V1 仍 1／6。**
不覆盖 PR #49 失败结论；原 PS5.1 问题、D1—D3、真实输入／费率及用户数据升级限制不变。


## WP-03／WP-06 本次推进：中断后的会话排空与生命周期锁（2026-09-17）

基线 `3a987bc6c1a5df608ff8f0668b866aaf87842c04`（PR #48）。本项独立修复托管会话
退出时中断等待、导致仍有在途工作却释放生命周期锁的问题，不改 PR #49 或其失败状态。

- 先在原实现上复现六项反例；关闭过程中发生 KeyboardInterrupt／SystemExit 时，保留
  首个异常并继续同一关闭等待。不是重新执行模型、领取、额度预留、SQL 或停机命令。
- 在途工作结束后，才按原所有权规则停止本会话启动的引擎；借用引擎不停止。随后原
  中断继续传播；停机本身失败仍为主错误，并保留中断异常链。未重新开放任务准入。
- 单独复审覆盖重复中断、借用引擎、业务异常及停机失败；整包证明验证真实锁与原
  数据库记录，故障仅为测试注入的 Python 异常，不发送系统信号或修改 Windows 策略。
- 原 67 份迁移、依赖、预算、选择／存储／结算算法和已有 CI 超时不变；最终源码树、
  测试结果、首败及未执行项记录于本次 PR。异常客户端仍须自身有界 I/O，不能保证
  硬终止、任意字节码处信号或所有非中断关闭错误的恢复。

**WP-03／WP-06 仍 PARTIAL，G3／G6 未关闭，V1 仍 1／6。** PR #49 的 PS5.1 首次调用
失败、被拦截的 CAPI2 试验及 D1—D3／真实业务验收均未绕过或宣称解决。无用户业务库、
密钥、真实模型／行情、旧安装覆盖或实盘操作；完成本项不授权部署到用户旧实例。


## 27. WP-03／WP-06：会话排空与任务回执的主线整合（PR #50）

整合基线 `839d4039406d1113676e0b474680e6d4061f289b`（已合并 PR #51）；保留
原 PR #50 `668c8eea2c6f740561afdfdd5368849ee58b4624` 的实现和历史，不重写分支。
原两份文档的追加内容均保留。PR #51 的输出／共享停止修复不回退；PR #49 不并入。

- 新增十二个组合反例：拥有／借用引擎、关闭中断／零退出、正常／短写／中断输出，
  均用真实会话与线程验证先排空、再释放锁、最后发布回执；原操作只执行一次。
- 这些新反例在未含排空修复的 PR #51 主线十二项全败，在整合实现中通过。只模拟
  原生引擎调用和检查载荷，不冒充真实数据库或操作系统信号验证；原整包测试另行执行。
- 不改两项已审阅的生产实现、原测试断言、67 份迁移、依赖、工作流权限或原超时。
  最终整合树的本地全量、Windows 分区／整包、独立复审和首败证据记录于本 PR，
  原 PR #50 或 PR #51 的旧树成功结果不得替代本轮最终门禁。

**WP-03／WP-06 仍 PARTIAL，G3／G6 未关闭。** 不关闭 PR #49 的失败、被拦截诊断、
D1—D3、真实输入／费率及完整业务验收，也不授权用户数据库操作或旧安装覆盖。


## 28. WP-02／WP-03 本次推进：已审阅批次与预算的显式命令入库（G2／G3 尚未关闭）

基线 `4df08873f4b9b8f1378913c0d00ca6bb183e51e2`（PR #44）。在既有任务命令增加
`enqueue-batch`／`create-budget`，直接复用原规范载荷、不可变存储和托管会话，补齐
批准输入的命令行入库步骤；不选择提供商、不调用模型、不新增文件队列或持久化层。

- 两个操作分别要求原编号、已审阅输入 SHA256 和各自明确写入许可；二进制 stdin 有界，
  只接受原规范输入及至多一个行结束符。不通过重新序列化、更新时间或默认参数补造许可。
- 回执只含原不可变元数据，不宣称新创建、当前队列状态或未使用预算；两次入库不是同一
  原子事务。重复原输入保留原时间，改内容冲突，失败后只能查询并明确原样重放，不自动重试。
- 单独反例自审复现返回值与入参共享可变引用时可绕过完整载荷比较的问题；固定调用前的
  原始规范载荷后校验，保留原断言和首败。任务控制台输出沿用单次完整写入／刷新检查，
  不把短写或输出失败视为成功，不从错误推断没有提交。
- 实际解压包组合场景改用这两条真实子进程命令入库，再验证没有任务领取或调用预留，
  并继续原有预算、研究、模拟、结算和重启流程；已使用预算重放不退额，变更额度冲突。
  精确最终代码树、单独工作副本复测、全量及适用 Windows CI 和未执行项记录在本 PR。

**WP-02 仍 AWAITING_OWNER，WP-03 仍 PARTIAL，V1 仍 1／6。** 哈希绑定不是输入批准或
费率认证，独立 `run-turn` 仍不装配真实客户端。D1—D3、真实预测／输入／费率验收、
安全版本切换和原 PS5.1 问题仍待办。无迁移／依赖变更、用户数据访问、密钥或实盘操作。

本轮首个 Windows 整包验证在后续模拟保存处失败（246 通过、1 失败），首败保留。
报告只有通用操作失败码，不把时间耗尽推断当成已证实根因。新增的三次入库重放与两项
已使用预算复核移至全部原输入保存之后，保留原截止、时刻、业务调用和断言，不延长窗口。
测试失败时另保留有限的合成时钟观察，避免嵌套异常显示截断；整包及所有最终版本门禁
仍须重新实际通过，此处不以离线测试代替原生结果。


### PR #47 与 PR #48／#50／#51 的主线整合（2026-09-17）

整合基线 `2a0e881a2057ce6cba33c1a6885dcd099d4a5b52`；保留原 PR #47
`f1d9f68d08fdb8a0453319e885c3827296d8d694` 的历史与首败。复用主线已有输出
函数和共享停止处理，不恢复旧的重复输出实现，不回退会话排空或原生三分区门禁。

- 合流反例先复现入库前输出中断不请求原停止令牌，再修复；单独自审进一步复现
  输入中断后错误回执也输出失败时停止信号丢失。先请求原令牌再发布错误回执，
  两份首败和原断言保留。普通输出错误仍不取消关联工作，已提交数据不回滚或重试。
- 只将两份入库测试模块显式追加到 dispatch 分区及整包验证；原 51 文件清单、
  自检模块、三个分区、汇总拒绝逻辑和全部原有测试／超时／权限保持。
- 整包仍走原审核输入、真实入库子进程、零领取／预留校验、已用预算重放、
  模拟／人工合成结算及中断保全；不刷新截止或更换输入凑通过。
- 最终固定树的自审、全量、原生／整包实际结果及未执行项记录在本 PR；旧树
  的成功不能替代整合验收。无新迁移、依赖、提供商、文件业务账本或用户数据操作。

**本项只推进审核入库操作接线，G2／G3／G6 和 V1 均不因此完成。** PR #49、
被拦截的诊断试验、D1—D3 与真实模型／来源／费率验收仍保留原限制。


## 29. WP-06 本次推进：整包业务历史的冷备份恢复验收（G6 尚未关闭）

基线 `4df08873f4b9b8f1378913c0d00ca6bb183e51e2`（PR #44）。在既有整包组合完成后，
继续使用其第二个隔离安装包的 Python／源码及原生数据库，串联原 backup／verify-backup／
restore 命令。不增加生产恢复接口、业务文件账本或自动升级器，不操作用户实例。

- 先核对预期实例和完整合成名册，停止后备份全部物理文件；校验实际归档哈希，验证已有
  数据禁止覆盖、错误校验和在创建恢复暂存前拒绝。模拟原文件不可用时保留完整私有副本，
  不删除原数据、不复制整个 .local、不改原物理根或迁移目录。
- 恢复后启动前逐文件核对物理字节，含原测试凭据及事务状态；启动后原预测、四份模拟、
  候选／确认、两批次／两轮次、八次预留和历史评估必须不变。显式原样重放不调用模型，
  incomplete 仍阻断当前严格评估，不因恢复而隐去失败、退还额度或重跑研究。
- 审核与单元反例、最终全量／Windows／实际 kit 证据在本变更 PR 留存。新增恢复子进程
  单独限时 180 秒，原测试／作业超时和所有原断言不放宽；原 67 份迁移、依赖及生产算法不变。
  备份和原集群包含私有测试凭据，只留隔离 runner，禁止上传到仓库或工程产物。

**WP-06 仍 PARTIAL，G6 未关闭，V1 仍 1／6。** 同版本同物理路径恢复不等于跨版本升级、
回滚或跨机器迁移；真实数据／费率／模型验收、D1—D3 与原 PS5.1 间歇问题仍待办。
没有用户本机任务、密钥发现、真实行情／模型请求、旧包覆盖或实盘操作。


### PR #46 与最新主线的冷恢复整合（2026-09-17）

整合基线 `41cc131d154a8a134d46608f5c5da1ca1e4ddaa6`；保留原 PR #46
`14c1bea70cfce3f3ae2dff40f93f5868f439933c` 的历史及首次 Windows 失败证据。
主线的审核入库、输出／共享停止、会话排空和备份目录前缀逻辑均不回退。

- 原 Windows 用例在恢复后的业务比较失败，原始日志／JUnit 保留。独立两次读取的
  反例复现了批次 `generated_at` 查询时间被误当持久化历史的问题；只排除该查询
  时间，原批次全部载荷／入库时间和完整领取／结果仍逐项比较，非删除批次检查。
- 使用真实批次快照类型复核；先经原构造器校验并复制，再比较持久字段。固定字段
  清单保证未来新增字段不能静默漏检；非法查询时钟、原时间／结果变化仍拒绝或不等。
- 仅整合测试和说明，不修改生产备份／恢复／研究算法、67 份迁移、依赖或原有
  工作流权限、选择与时限。恢复测试只使用既有第二个私有合成实例及实际命令。
- 原生整包必须同时证明启动前物理文件一致、启动后业务历史一致、已用预算不返还、
  incomplete 不消失及原样重放不调用模型。最终固定树自审和 CI 证据保存在本 PR；
  原候选或其他 PR 的绿标不能替代本次验收。

**WP-06 仍 PARTIAL，G6／V1 未关闭。** 不宣称跨版本／跨机器升级或真实灾难恢复，
不绕过 PR #49／受阻诊断，D1—D3 与真实模型／来源／费率验收仍待办。


## 30. WP-02／WP-03：固定输出额度冲突在任务领取前拒绝

基线 `34e9f3bf7d08650714754c757a9e4a9dff16be92`（PR #46）。复用原预算执行入口，
在已准备的新请求的单次输出 token 上限超过预算许可时，先拒绝而不消耗不可重跑的领取。
不修改请求上限凑适配，不自动换预算；后续明确提供匹配的获准预算仍使用同一原请求。

- 原实现反例确认已领取并保存为 `model_failed`，虽未调用真实客户端却消耗了原任务；
  现在通过已有只读查询保留完成／incomplete 重放，无旧记录时返回固定配置错误。
- 原已阻断且不需模型的 intake 保存路径保留；额度耗尽／过期错误优先级不变。
  每次真实调用仍须通过原事务预留、输出上限／消息字节检查，静态预检不代替它们。
- 同一原生预算用例追加混合批次、零领取／零预留、显式匹配预算及已有记录重放，
  只使用隔离 CI 数据库和合成客户端。原迁移、收费算法、权限和 CI 选择／时限不变。
- 最终固定版本的单独自审、完整离线和 Windows／kit 证据记录于本 PR；
  本地通过不代替尚未执行的原生场景，不把配置拒绝补写为预测记录。

**WP-02 AWAITING_OWNER，WP-03 PARTIAL，G2／G3／G6 和 V1 未关闭。** 只关闭
不可兼容输出配置的提前拒绝子项；不认证提供商、费率或真实预算，不绕过 PR #49
及受阻诊断；D1—D3、真实输入／费用／业务验收和用户数据操作授权均未改变。


## 31. WP-02／WP-03：首次消息字节超限在任务领取前拒绝

基线 `a1284c41db411281b666211c6a9ce4b6930ca8ec`（PR #52）。补齐与固定输出
额度预检相邻的消息大小缺口：已知首次实际消息超过预算限制时，不先消耗不可重跑的任务。

- 原实现反例复现领取后保存为 `model_failed`、但客户端零调用；现在复用原 Agent
  初始上下文构造并按 UTF-8 字节检查，返回固定配置错误，不截断原请求或自动换预算。
- 原空／过期预算与输出额度错误优先级保留；不需要模型的 intake／证据／上下文阻断
  仍走原保存路径。已有完成／incomplete 原样重放，历史查询异常不进入新领取。
- 首条刚好达到上限可以调用；后续消息仍受原逐次事务预留约束，不能据此保证整轮费用。
  现有原生预算用例追加两团队零领取／零预留、明确匹配预算及中断历史重放证明。
- 原消息内容／顺序／模型循环、67 份迁移、依赖和工作流不改；最终固定树的独立阶段
  自审、原始首败、全量与 Windows／kit 证据在本 PR 留存，未执行项不冒充通过。

**WP-02 AWAITING_OWNER，WP-03 PARTIAL，G2／G3／G6／V1 不因此完成。**
不认证提供商、实际费率或完整请求传输大小；D1—D3、PR #49 及受阻诊断限制不变。
不访问用户数据库或凭据，不调用真实模型／行情，不授权部署或覆盖旧安装。


## 32. WP-05：模拟回执绑定原审核输入（候选，尚待最终原生验收）

基线 `a71ae4d716e8a73bd3614f080f42e5a6968d76f8`（PR #53）。本候选修复
模拟保存命令用调用后的可变参数比较回执的问题，不新增模拟器或业务存储。

- 六个原实现反例复现：故障适配器改变数量、费率或假设后返回内部一致的回执，
  命令仍成功。现在绑定原 `input_sha256`，不让被改动的参数成为自己的审核依据。
- 单独自审覆盖正确原回执与参数后改动、清理时回执对象变化；保持原回执投影，
  不误报正确保存失败。异常仍保守保留可能写入，不补发回执或自动重试。
- 原有原生模拟命令用例追加 BTC／ETH 实际同编号重放，故障仅在真实存储返回后
  改动适配器参数；最终仍核对原记录、模拟数量及迁移账本。原 CI 选择与时限不变。
- 67 份迁移、依赖、工作流、输入编解码、模拟与数据库算法不改；本地反例／自审
  不代替最终 Windows、原生 PostgreSQL 和实际安装包验证。固定证据在交付记录中。

**WP-05／WP-06 仍 PARTIAL，G5／G6 与 V1 未关闭。** 此项不证明恶意 Python
隔离或真实费率／盈利，不推断错误后回滚，不处理 PR #49 或受阻诊断；D1—D3、
真实输入／人工核验及用户数据操作授权不变，禁止覆盖旧安装或调用真实模型。


## 33. WP-03／WP-04／WP-05：只读查询与评估回执完整性

基线 `a71ae4d716e8a73bd3614f080f42e5a6968d76f8`（PR #53）。补齐恢复操作的查询端：
任务查询、任务清单、结算查询及两种评估不得把内部零退出或不完整输出当成可靠回执。

- 复用既有结算输出函数，一次完整 JSON 写入并检查字符数、显式刷新；序列化／输出失败
  返回非零，不追加第二份信封，不重新查询或重放业务操作。正常输出与未找到的退出码保持。
- 非结算评估等原只读路径捕获内部 SystemExit，保留参数帮助及操作 KeyboardInterrupt
  的原行为；异常原因只从原固定字符串参数识别，不执行异常对象的自定义文本转换。
- 单独自审复现概率评估异常文本转换逸出零退出，修复后保留原断言。21 组正常及普通
  失败路径的输出、退出码和生命周期顺序逐项一致；测试不改变原评分／历史完整性约束。
- 原生库存用例追加五次真实只读命令的短写注入，复查原记录、行数、引擎身份及停止状态。
  只改 CI 隔离测试的 stdout，不模拟数据库，不增加原超时；完整用例和最终门禁须通过。
- 原迁移、依赖、工作流、领域计算及数据库读写算法不变。最终版本、单独自审、完整
  本地／Windows／安装包结果与未执行项记录于本 PR，不能用局部通过代替最终验收。

**WP-03／WP-04／WP-05／WP-06 仍 PARTIAL，G3—G6／V1 未关闭。** 输出完成不等于
消费者已收到，输出失败不证明之前操作回滚。D1—D3、真实模型／输入／费率验收、PR #49
及受阻诊断限制不变；不操作用户业务库、凭据或旧安装，不自动修复／重试／调用提供商。

### 本轮首次托管验收发现并修复的测试输出生命周期问题

首次 paper 流程表面成功，但日志／JUnit 数量不一致且遗漏 93 个原用例，不能验收。
根因复现为测试的 BrokenPipe 注入在 pytest 无捕获运行时仍覆盖真实 stdout；缩短注入
作用域，在返回测试运行器前恢复原流，原断言保留。新增捕获／不捕获两种真实子进程
回归，核对完整用例编号和 JUnit 数量；首次不完整报告及反例保留，最终新版本须重新
完整验收。未改生产逻辑、原超时或测试选择，也未将旧流程追认为成功。


### PR #55 与 PR #56 的固定版本整合（2026-09-17）

整合基线 `0adfadaccb4e310c67fcd3a78455f38d2e39d97b`，原 PR #55 候选
`dc6a5a0cf6b149bfcab065fac36ee9f90c6e7336` 保留为父提交。保留第 32 节模拟
回执原审核哈希绑定，将原只读输出章节顺延为第 33 节，不覆盖任何原业务修复。

- 生产四个只读命令与原 PR #55 修正后版本字节一致；PR #56 的模拟保存命令、
  原模型／预算／结算／存储算法、67 份迁移、依赖与工作流保持主线字节不变。
- 新增模拟保存／任务命令优先导入的两种独立进程验证；另在捕获与不捕获模式下，
  先运行输出故障，再运行全部 14 项原审核回执反例，要求 15 条完整且成功的 JUnit。
  不以日志尾部或工作流绿标代替完整用例清单，也不重复计入嵌套子进程覆盖量。
- 保留原首份不完整托管报告、输出注入生命周期修复及其反例；本轮最终固定树必须
  重新通过全量、Windows 原生与安装包门禁。阶段自审和实际结果固定记录在原 PR #55。

本项只推进原查询／评估输出接线，G3—G6、V1、D1—D3、PR #49 与受阻诊断限制不变。
无自动重试、超时放宽、用户数据库／密钥／真实提供商／旧安装操作。


## 34. WP-03／WP-04／WP-06：原编号恢复查询的异常与完整输出

基线 `a71ae4d716e8a73bd3614f080f42e5a6968d76f8`（PR #53）。修补按执行编号／
审核编号查询时内部零退出和输出不完整仍可能表现为成功的缺口，复用已有输出函数。

- 两条只读入口在原托管会话退出后完整序列化、一次写出、核对长度并刷新；内部
  SystemExit 记为原固定查询错误，输出失败非零退出、不追加第二份信封、不自动重查。
- 原查询与清理中的中断仍原样传播，不编造查询结果；正常 JSON 和查无记录退出码不变。
  输出失败不证明记录不存在或此前写入已回滚，不授权换编号或重跑业务。
- 查询、摘要校验、原事务和清理代码保留；延迟导入避免与确认模块的已有反向引用形成
  初始化循环。单独阶段复审覆盖序列化故障及两种真实解释器导入顺序。
- 原生确认用例在前瞻采集结束后追加真实查询子进程短写、原样重查和记录保全；只在
  隔离 CI 数据库运行。最终固定树、实测及未执行项由本 PR 记录，不用本地代替 Windows。

**WP-03／WP-04／WP-06 仍 PARTIAL，G3／G4／G6／V1 不因此完成。** 原 67 份迁移、
依赖、CI 选择与时间限制均未修改；PR #49、受阻诊断、D1—D3 与真实输入／费用授权不变。
不操作用户数据库，不调用真实模型／行情，不部署或覆盖旧安装。


## 35. WP-04：确认回执固定绑定原审核输入（不关闭 G4）

基线 `80d64b6b081caee385aca64dbf2e086144b016f8`（PR #54／#55 整合）。
修补确认命令与服务在适配器调用后再比较可变参数、可能误接纳改写审核的缺口。

- 24 个完整故障适配器反例先在原代码失败：改动审核编号、审核人、来源文本／引用、
  时间、原记录或 YES／NO 后返回内部一致的回执，不能替代原审核。最初四项测试
  引用格式不合规已单独保留并纠正；不把夹具构造失败算成已复现产品缺陷。
- 命令复用原 `copy_review` 传入分离的嵌套参数；服务在原原子写入前固定规范化载荷。
  正确原回执不因参数后改动被误拒；不重试写入、不换编号，也不据错误宣称回滚。
- 单独自审覆盖正确回执保全、清理后的结果投影与绑定失败前零写入；原解码、来源／
  时间／结果准入、67 份迁移、依赖和工作流均不改，不扩展来源认证或恶意代码隔离。
- 原生确认用例追加 BTC／ETH 原编号实际重放，只在真实写入返回后注入参数改动；
  后续原记录、候选、结算行数、迁移及实例检查仍须完整通过。最终固定版本自审与
  完整 CI、实际安装包证据在本 PR 登记，标记行和本地通过不替代最终原生验收。

**WP-04／WP-06 仍 PARTIAL，G4／G6／V1 不因此完成。** D1—D3、真实预测、
来源／费用及独立人工核验仍待；不处理 PR #49 的失败或受阻诊断，不操作用户数据库、
凭据、真实模型／行情或旧安装。此处是故障适配器一致性修复，不是实际用户数据损坏报告。


## 36. WP-06 本次推进：托管研究会话复用安装包完整性校验（G6 尚未关闭）

基线 `3a987bc6c1a5df608ff8f0668b866aaf87842c04`（PR #48）。修复只经启动入口
校验安装包、直接研究命令却可漏检的问题；复用现有 verifier，不新建安全／诊断系统。

- 每次进入托管会话，先核对实际源码目录及数据根目录中的 kit；任一清单或引擎种子
  标志存在就必须完整校验。缺件、改写、额外源码／SQL、链接及不可读取标志均阻断，
  在私有状态、生命周期锁和引擎访问之前结束；同一路径去重，不缓存跨会话成功结果。
- 无标志的 SOURCE 安装保留原行为；不认证恶意代码或删除全部标志，不承诺文件系统
  原子快照或会话期间监控。显式 status/down、原权限和持有／借用引擎清理规则不变。
- 单独自审复现不可读标志的系统异常外泄，改为固定错误；首败及原断言保留。实际
  Windows kit 验证直接命令、不同数据根、停止与已运行引擎拒绝及历史保全；最终固定
  树、测试结果和未执行项记录于本 PR。原 67 份迁移、依赖及业务算法不改。

**WP-06 仍 PARTIAL，G6 未关闭，V1 仍 1／6。** 此项不部署用户安装、自动修复文件、
配置模型或关闭原 PS5.1、D1—D3、真实输入／费率及业务验收；无用户数据操作。


### PR #49 与 PR #57 主线的完整性／排空／冷恢复整合（2026-09-18）

整合基线 `e1e47df60a59207575b7a266902e0a99ced9d283`；原候选
`061a55703197cc526f3d707d9da58b971c582e0b` 和首次 PS5.1 超时保留，
不是重跑原失败作业或宣布其根因修复。原第 36 节为历史候选说明。

- 只把已有入口校验接到当前会话，完整保留主线排空、借用引擎所有权、
  原审核回执、整包业务流程及同版本冷恢复；不回退任何后续主线改动。
- 新增 22 项组合反例覆盖校验先于私有访问、真实线程排空、原异常链、
  停库失败优先级，以及校验后文件改变仍排空已准入工作、下一次重新拒绝。
  不将入口检查说成原子文件系统快照或持续监控，不把 Python 异常注入说成 OS 信号。
- 原生／整包保留全部旧断言与时限；本轮固定树须重新跑完整离线、
  三分区＋汇总、paper 和实际 kit。旧候选的失败仍是失败，不能以兄弟作业通过替代。
- 独立阶段自审、首轮组合检查、所有最终日志／哈希和未执行项登记于 PR。
  D1—D3、G4／G6／V1、真实输入／费用及原 PS5.1 可靠性问题不因此关闭。

没有用户安装变更、用户数据库操作、密钥访问、真实模型／行情调用或受阻诊断。
