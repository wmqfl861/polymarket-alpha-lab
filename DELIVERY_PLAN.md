# V1 交付总计划：BTC／ETH 研究与模拟验证版

状态：**WP-01 的 G1 候选到待审入口已验收；其余五包未完成；不是 V1 发布完成声明。**
计划基线：`bd8f3c50bef29f0b3c64a260bf99f23d739684f4`（PR #26）。
基线日期：2026-09-14。用户已同意先收敛 BTC／ETH 首版，再扩展其他团队。
维护规则：每个相关 PR 同步更新本文件的状态、缺口、依赖及固定版本证据。

## 1. 唯一的当前交付口径

本文件是当前 V1 的范围、工作顺序和结束条件的唯一总表；它不是又一个独立报告模块。
`AGENTS.md` 仍管理工程、安全及授权约束。旧路线图继续作为长期方向和历史依据，
但旧的 Supabase 部署要求、外部审核要求、仅 BTC 可运行等描述不能覆盖当前项目规则。
本计划不授权读取密钥、付费模型调用、用户业务库写入或实盘交易。

**V1 的终点：** 普通用户在 Windows 上通过一套明确入口，完成受支持 BTC／ETH 市场的
筛选、规则审阅、获准模型研究、记录保存、任务运行管理、人工核验结算和成本感知模拟评估；
能识别失败与未知状态，能停止及重启且不丢失历史，能安装一个固定版本的可追溯发布包。
“输入 prepared”“代码合并”“测试全绿”都不单独等于这条完整路径已验收。

项目只有一个本期产品里程碑：**V1-Research-Paper**。下面 WP-01 至 WP-06 是六个工作包，
不是六次对话、六个文件或六个 PR；不再把每个查询器或修复称为一个新的产品阶段。

## 2. 首版范围与明确不做的事

| 纳入 V1 | 留到下一版或单独授权阶段 |
| --- | --- |
| `crypto_btc`、`crypto_eth` 两个团队；复用现有通用研究循环 | 另外八个团队的领域采集、连续运行及真实业务验收；十团队长期目标保留 |
| 显式 Yes/No、受现有合约形态和实际观察时间门禁支持的固定时点价格事件 | 路径依赖触价、期间最高／最低、区间统计、未知或矛盾规则；不为凑候选放宽门禁 |
| 项目私有原生 PostgreSQL、现有迁移账本、Windows 本地使用 | Docker、Supabase 服务、共享外部数据库、其他耐久业务存储 |
| 人工审阅规则、显式批准研究输入和模型；有界任务运行 | 自动批准模型输出、自动认证来源或链上最终性、任意工具／网址／代码执行 |
| 真实研究接入、失败留存、结算证据、模拟成交成本与评估 | 实盘、账户／钱包、签名、真实订单、自动仓位或资金分配 |
| 一个统一操作流程；优先复用现有命令行和组件 | 新 Web 仪表盘、重复健康报告／门禁／摘要层、框架重写和微服务拆分 |
| Windows 可追溯安装与保全旧数据的版本切换说明 | 原地覆盖旧 kit、复制 `.local` 冒充迁移、自动删库修复、无条件自动升级器 |

来源适配是准入条件而不是口头免责声明：Coinbase／Kraken 的 USD 小时行情只能作为研究输入，
不能当成 Binance USDT 分钟结算数据。规则审阅和结算核验必须明确引用合约实际指定的来源、
交易对、时间和粒度。此版允许人工独立取得结算证据；不要求先开发所有结算源的自动历史采集器。
无法核验的事件保留未决，不算已结算，也不纳入“已完成样本”。

## 3. 已有能力不是从零重写

| 可复用基础 | 基线证据／使用说明 | 不能由此推导的结论 |
| --- | --- | --- |
| 项目私有数据库启停、迁移、备份与权限控制 | [database/README.md](database/README.md)、[quickstart](database/quickstart.md)；PR #10—#14 | 用户业务库已升级、已有真实预测或新 V1 已发布 |
| 公开发现、双源预览、合约／观察时刻检查 | [发现](docs/research-crypto-discovery.md)、[启动](docs/research-crypto-launch.md)、[合约](docs/research-crypto-contract-scope.md)、[时刻](docs/research-crypto-observation-time.md)；PR #16—#22 | 第一个搜索结果可研究、来源等价、当前结果已获批 |
| 通用有界模型／工具循环、批量并行及任务收录 | [研究 Agent](docs/team-research-agent.md)、[启动](docs/research-crypto-launch.md) | 十团队真实运行完毕、常驻调度已完成、提供商已选定或已获付费授权 |
| 任务查询、完整有界清单及评估入口 | [查询](docs/research-execution-inspection.md)、[清单](docs/research-execution-inventory.md)、[评估](docs/research-evaluation-console.md)；PR #23—#25 | 查得到记录就能自动重跑、无记录等于全库为空、没有未完成任务等于预测有效 |
| 未决市场清单、候选证据、人工确认和单条结算查询 | [清单](docs/research-resolution-queue.md)、[确认](docs/research-resolution-review.md)、[查询](docs/research-resolution-inspection.md)；PR #13、#15、#26 | 自动确认最终性、旧候选被后来结果升级、真实结算全链路已验收 |
| 模拟成交、成本、风险和验证规范的既有组件 | [验证门槛](docs/research/validation-gates.md)、[长期路线](docs/superpowers/specs/2026-06-13-automated-investment-roadmap.md) | 这些组件已接入新研究主流程，或已有可信收益证据 |

证据分类必须保留：`代码已合并`、`离线验证`、`隔离原生集成`、`真实公开输入`、
`用户报告的本机检查`、`获准真实模型`、`完整业务验收`。一种不能替代另一种。
PR #26 的全量、Windows 和 kit 测试是组件工程证据，不是六个工作包的完成证明。

用户此前报告本机研究清单为零，之后报告了公开预览成功及受控网络重试恢复；
本计划没有重新查询其机器，也不把这份历史报告当成数据库“现在仍为空”的实时结论。
此前未发布的候选筛选器／模型预检只算未交付候选，不计入 main 的能力或本期完成量；
曾被平台拒绝的写入不得通过换接口、编码或代传绕过。

## 4. 六个工作包总验收表

状态词：`NEXT` 下一实施项；`PARTIAL` 有基础但未完成；`AWAITING_OWNER` 缺用户决定；
`IN_PROGRESS` 正在实施；`WAITING` 等待真实事件或证据；`BLOCKED` 具体障碍；`DONE` 全部完成条件及证据齐备。
“工程基础已有”不是 DONE。下表是基线状态，不是虚构的完成百分比。

| ID／工作包 | 基线状态 | 剩余缺口 | 依赖 | 完成证据 |
| --- | --- | --- | --- | --- |
| WP-01 候选到研究入口 | **DONE（G1）** | 有界发现、最新规则／观察时刻、一次完整预览和待审规格已接通；本项不包含模型授权或来源真伪认证 | 现有公开输入、PR #19／#20 门禁 | [PR #28](https://github.com/wmqfl861/polymarket-alpha-lab/pull/28)：52 项反例测试、三条 CI、真实 BTC／ETH 待审样例；详见第 10 节 |
| WP-02 真实模型与预算 | **AWAITING_OWNER** | 已实现同库共享调用额度预留和惰性客户端接线；仍需提供商／实际收费上界验证／数据授权／预算决定及真实研究收录 | D1—D3；可先离线开发，不需要先拿密钥；真实验收依赖 WP-01 | G2：两团队分别至少一次获准真实研究；原失败保留；重复请求不再次调用模型；授权与使用可核对 |
| WP-03 任务调度与恢复 | **PARTIAL** | 持久批次／公平轮转／共享额度已接统一受控入口：查询批次、轮次和预算，显式运行一轮与停止恢复；独立脚本不装配真实客户端。仍缺获准提供商／收费上界验证及最终 G3 组合验收 | 复用现有领取／收录；先合成模型，真实运行依赖 WP-02／D3 | G3：两轮调度＋一次停机重启＋混合失败／中断场景，任务不丢失、不重复执行、不隐去 incomplete |
| WP-04 结算闭环 | **PARTIAL** | 已接原预测／候选哈希绑定的人工确认入口，核对原规则、声明来源与分钟时刻，并复用原子结算保存；仍缺真实 BTC／ETH 前瞻预测、实际匹配来源和独立人工核验 | WP-01 支持事件；WP-02 真实记录；事件到期和独立人工核验 | G4：BTC／ETH 各至少一个真实事件保留预测到结算的完整证据；过期、争议、拒绝和未决路径可追踪 |
| WP-05 研究到模拟评估 | **PARTIAL** | 已将原始严格研究评估接入现有订单簿撮合及成本边际计算，保留所有尝试／拒绝／缺口；仍缺获准行情与成本场景的耐久收录、真实双团队样本和完整 G5 验收 | WP-02、WP-04；订单簿和成本记录；可先做离线组合 | G5：两团队至少各一个完整工程样本；拒绝项、样本缺口、成本和时间均可复算，不据此声称策略有效 |
| WP-06 操作与发布收尾 | **PARTIAL** | 统一说明、配置、安全版本切换、长期 Release；PR #30 保留的 PS5.1 首跑超时根因仍未定位 | 文档整理现在可做；最终验收依赖 G1—G5 | G6：固定版本 Windows 全流程＋负向恢复验收＋发布资产哈希；无本机补丁拼装、无覆盖旧数据 |

**当前完整验收：1／6（仅 WP-01／G1）。** 这是六个端到端工作包的状态，不表示现有代码完成量为零。
每项达到 DONE 必须填入确切 PR、head／merge／tree、执行命令、结果、适用平台和未执行项；
真实业务记录在项目数据库私下留存，GitHub 只放已审阅的脱敏验收摘要。

## 5. 每个工作包的停止条件

### WP-01：先解决主流程，不新增旁路报告

- 只处理有界搜索结果；检查最新规则和实际取价时刻，保留失败、拒绝、未检查、无候选的区别。
- 明确请求总上限及每次重试原因。到截止时间停止下一步，不延长截止时间凑成功。
- 待审规格携带市场 ID、条件 ID、规则绑定、研究时间窗及来源用途；筛选不等于批准。
- 不支持的规则、源不匹配或已发生观察不得启动研究。最后启动前再次验证，避免继承旧资格。
- 完成 G1 后转 WP-02／WP-03；额外排行榜、报告或健康摘要不在这个工作包内。

### WP-02：有授权才有真实模型验收

- D1—D3 必须由用户明确决定。模型客户端的标签不证明实际提供商，编码 Agent 的登录不等于项目配置。
- 实现前先审阅凭据隔离与数据发送设计；实际凭据只在用户批准的本地运行边界提供，不进入聊天、GitHub、报告或研究证据。
- 新任务先记录前瞻性请求和领取，再执行模型，完整保存成功／阻断／失败；输入无效时不浪费付费调用。
- 同一任务重放返回原结果；不自动重试付费模型或借换 task ID 绕过 incomplete。恢复路径不抹掉首次失败。
- 调用次数、并发和请求大小在发送前受限；费用必须有可执行约束。响应后 token 统计不是硬金额封顶。
  无法落实用户要求的预算边界时，真实执行保持 BLOCKED，不以“尽力而为”冒充限额。

### WP-03：连续运行有明确边界

- 复用项目数据库、原任务身份和领取协议。队列运行元数据如需持久化，只能使用同一项目 PostgreSQL。
- 显式启动和停止；不默认注册系统服务、不无限重试、不从模型回答产生新任务。
- 先用合成模型验证正常轮次、部分失败、截止到期、进程中断、重启和并行重复请求；故障注入不使用付费模型。
- 不靠记录年龄猜 worker 已死并重新执行；丢失结果保留 incomplete，原评分完整性约束继续生效。
- G3 的场景验证说明可运维，不宣称无限期稳定、严格实时或跨机器分布式能力。

### WP-04：可人工核验，不假装自动最终性

- 复用既有候选采集、审核提交和原子结果保存接口；结算来源必须匹配合约指定的市场／时间／粒度。
- 人工取得实际来源证据是首版允许的流程。自动全历史采集、链上验证及未知类型解析推迟。
- 原预测须早于观察时刻；确认和未确认记录不可混同，后来结算不能改写旧候选。
- 争议、50/50、错误来源、时间冲突、缺失证据保持未决或阻断；不能默认 NO、自动覆盖结果或补造历史。
- 真实事件未到期或无法独立核验时记录 WAITING，不通过合成结算宣布 G4 完成。

### WP-05：软件闭环与策略有效性分开

- 先核对已存在组件，再做必要适配。模拟成交使用可执行 bid／ask、深度、手续费和可说明的滑点；不以 midpoint 代替可成交价。
- 实验名义数量是明确的模拟参数，不是资金建议或真实下单量。无真实订单、钱包或自动头寸配置。
- 保留全部研究尝试和模拟拒绝项；按一致时间、模型、协议版本和事件分组，展示样本分母、缺失结算及成本。
- 每团队至少一个真实工程样本仅证明接线可运行，不证明校准、收益或统计显著性；样本不足必须明示。
- 原 [Gate 0—8](docs/research/validation-gates.md) 不被此处替代。进入更高等级的
  200 次候选观察、50 次模拟交易、30 次已结算／退出交易，以及深度策略至少四周前向采集等
  既有起始门槛仍适用；V1 软件可交付不等于策略获晋级或实盘获授权。

### WP-06：固定版本交付，而不是继续累加功能

- 一份 quickstart 串起配置、发现／审阅、研究、任务管理、结算和评估；优先组合现有入口，不预设新 UI。
- 使用同一固定版本跑完整隔离 Windows 路径，验证停止／重启、错误配置、网络失败和无业务写入的只读检查。
- 正式本机验收只安排云端无法替代的步骤；不反复重跑已经通过且未受影响的数据库、网络或下载检查。
- 发布长期可达的明确标记研究／模拟用途的 Release：原始测试 ZIP、SHA256、源码 commit／tree、依赖和升级限制。
- 旧运行时及数据库保全；新源码可经审核指向原物理项目根。旧 kit 不覆盖解压，任何数据迁移另行设计与授权。
- 自审发现清零、最终差异与最终测试树一致后才给 V1 结论。既有未执行检查、平台限制、预算和数据限制随版本说明。

## 6. 实施顺序、并行与完成判据

关键路径：**WP-01 → WP-02 → WP-04 → WP-05 → WP-06**。
WP-03 可在现有领取／存储接口上先做离线设计和实现，但必须在最终交付前接入；
WP-02 的无凭据装配设计、WP-04 的人工操作流程、WP-06 的文档整理也可并行，写入范围不得冲突。
模型决定未到位不阻止这些安全的工程工作，不能反复把“找密钥”交给本地 Agent。

WP-01 已按第 10 节关闭 G1，不继续扩充筛选排行榜或旁路报告。
**下一实施重点：WP-02 获准客户端／收费约束与待决授权；其间推进 WP-04 结算衔接和 WP-05 离线组合。WP-03 受控入口已接通，真实模型组合验收仍待办。**
真实模型执行仍等待 D1—D3；可先用合成输入验证，不要求用户重复网络或数据库验收。
先核对允许的发布路径和已有代码；不得把历史未发布工作假定为现成依赖。
若写入被平台拒绝，记录具体障碍，不换接口、编码或本地代传绕过；可继续不依赖受阻内容的工作包。

每个 PR 开工前写清 `WP-ID + 要关闭的验收缺口 + 最小改动范围 + 所需授权`。
开发完成后由协调者作单独自审及反例验证，不宣称第三方／全新 Agent 审核；
相关测试和适用的最终 CI 必须通过，同步总表证据，再合并。
只修首版阻断性问题或本次改动引入的缺陷；其他增强登记在文末延期区，不自动扩大首版。
发现会改变范围或完成条件的需求时必须重新取得用户同意，不能悄悄降低门槛或增加第七个工作包。

**发布判据：G1—G6 全部有可复核证据，D1—D3 已明确且对应授权受遵守，
无未解决的首版阻断缺陷，同一发布版本通过最终集成与自审。**
未达到前只能报告“工作包推进／组件完成”，不能报告整个阶段完成。
到达后冻结 V1；统计样本积累和后续领域扩展使用新的里程碑，不自动变成本期永远做不完的尾项。

## 7. 用户待决事项与授权边界

| 决定 | 当前状态 | 需要的非敏感信息 | 影响 |
| --- | --- | --- | --- |
| D1 提供商和准确模型 | **未知，未选定** | 提供商、可用接口／模型标识、获准客户端工厂或装配方式 | 阻断真实模型验收，不阻断离线设计 |
| D2 研究输入发送许可 | **未授权／未知** | 是否允许把选定市场公开问题、完整规则和批准证据发给该提供商 | 没有明确许可不发送 |
| D3 首轮规模和费用约束 | **未知，未确定** | 事件数、每任务最大模型请求数、并发、总预算及运行频率 | 没有可执行预算不开始真实运行 |

不在聊天中索要 API 密钥，也不探测是否存在 `.env` 或用户凭据。用户同意此交付计划，
只冻结产品范围和开发优先级，不自动等于批准上述数据发送、凭据处理或业务执行。

## 8. 进度更新与计划可信度

上轮提出的工期是粗粒度资源估算，不是承诺日期、后台执行安排或已经验证的进度。
范围现在才冻结；在 WP-01 合并和 D1—D3 确认后，用实际工作量、阻断时间与可并行性重估。
不按测试数量、PR 数量或对话轮数换算完成百分比；工程人日与等待事件到期的日历时间分开。
长期十团队和实盘路线保留，但没有自动排进当前六包或获得实施授权。

以后每轮交付摘要采用同一口径：
**本次 WP／关闭了哪条 G 的子条件 → 固定合并版本与实测 → 工作包尚余缺口 →
六包状态是否改变 → 下一关键路径动作／真正需用户或本地执行的事项。**
没有真正本地必做项就不派发任务；有文件交接则遵守 GitHub 固定提交＋校验＋完整提示词规则。

## 9. 延期区与历史来源

延期：另外八团队、广泛路径依赖历史采集、自动最终性验证、新仪表盘、额外诊断叠层、
框架重写、分布式多机工作者、跨机器数据库迁移、实盘与自动资金配置。
严重缺陷不是延期增强，仍需在受影响功能启用前处理，但不把局部修复另命名为产品阶段。

参考历史，不作为当前交付状态：
- [2026-07-12 进度路线图](docs/roadmap/2026-07-12-project-progress-roadmap.md)
- [策略团队架构待办](docs/strategy-team-architecture-implementation-backlog.md)
- [自动化 Level 0—5 长期路线](docs/superpowers/specs/2026-06-13-automated-investment-roadmap.md)

本计划建立只涉及文档和项目规则，没有部署调度器、批准模型、登记市场、写入用户数据库、
运行真实研究或发布新应用包。表中空缺必须由后续实际工程和授权验收填满，而不是靠改状态文字。

## 10. WP-01／G1 验收记录（PR #28）

**G1 已关闭：候选到待审规格已接通并实际验收；不等于研究已经获批或模型已运行。**
本轮实现基于 PR #27 `4618fb51af4066be26b0f09cc7a328d4e9888828`。
运行代码提交 `daa523b17111ff55f8048fd69fb6c6b4581426c9`，代码树
`d74116b5a84075d30ff27e74ddadef45b2b41eca`。
其后只更新本总计划记录验收，运行代码、测试与工作流不变；最终 head／merge／tree 及
最终提交的重复 CI 结果固定记录在 [PR #28](https://github.com/wmqfl861/polymarket-alpha-lab/pull/28)。
合并仍须该最终提交适用的 CI 全部通过，不能用这里的前一提交测试代替失败的最终门禁。

### G1 对应能力和单独自审

- 在已有 `scripts/discover_crypto_research.py` 增加显式 `--select-supported`，不是新建旁路查询／报告命令。
- 一个搜索页、有界详情、既有合约／实际时刻门禁、首个合格项的一次重新取数预览及未批准的待审规格。
- 不延长 cutoff，不重试详情／预览，不尝试第二份完整预览；失败、拒绝、未检查、无候选分别保留。
- 自审两项反例先失败再修复：缺失开放状态不能当作完整负面证据；搜索尝试轨迹不得超过调用方预算。
- 新增 **52 项合成测试通过**，相关回归 **381 项通过**，另有 **648 组有限状态／顺序／计数对照**通过。
  这些是同一协调者的单独自审和有限验证，不是第三方审计或无缺陷保证。
- 本地完整验证 **37040 passed／31 skipped**；Python 3.13.5／pytest 9.0.2 使用容器现有依赖，
  不冒充 GitHub 锁定环境。日志 SHA256：`58bc767c3fcf6c6eea38003cff9cef300bbeb6535ea73a34639a52865b56c8a4`。

### 固定运行代码的仓库验收

| 验证 | 实测结果 | 固定 run |
| --- | --- | --- |
| 锁定依赖完整离线验证 | 37040 passed，31 skipped；安装／入口／编译及源码保全通过 | [34847301418](https://github.com/wmqfl861/polymarket-alpha-lab/actions/runs/34847301418) |
| Windows 原生数据库及相关回归 | 1300 passed，2 skipped；52 项新测试全部执行，无失败／错误 | [34847301431](https://github.com/wmqfl861/polymarket-alpha-lab/actions/runs/34847301431) |
| 实际 Windows kit 构建／解压／启动 | 96 passed，0 skipped；两个解压环境均通过新选项禁用模式 | [34847301533](https://github.com/wmqfl861/polymarket-alpha-lab/actions/runs/34847301533) |
| 真实公开候选到待审检查 | BTC、ETH 均 ready_for_operator_review；不调用模型／数据库，不批准研究 | [34847442073](https://github.com/wmqfl861/polymarket-alpha-lab/actions/runs/34847442073) |

31 个默认跳过项中，18 项 PowerShell 和 9 项原生检查在对应 Windows 流程执行；
4 项旧数据库 opt-in 本轮未执行。两个 Windows 跳过项为既有特权符号链接测试。测试选择有重叠，不能相加。
原生数据库和应用包使用隔离实例及合成业务输入；真实公开检查是另一项证据，未访问用户业务库。

### 真实 BTC／ETH 支持实例（2026-09-14 13:09 UTC）

在 Ubuntu 工程 runner 实际执行一次，未在外层重试（不是用户机器的运行）：

```bash
.venv/bin/python -I scripts/discover_crypto_research.py --team crypto_btc --team crypto_eth --select-supported --max-candidates 10 --attempts 3 --allow-public-fetch
```

| 团队 | 实际市场 slug | 初筛／完整预览 | 观察时刻与临时截止 |
| --- | --- | --- | --- |
| BTC | bitcoin-above-68k-on-september-14-2026 | 131 个搜索候选，检查 1 个、130 个未检查；一次完整预览，待人工审阅 | 观察 16:00 UTC；临时截止 13:39:18.874366 UTC |
| ETH | ethereum-above-2000-on-september-14-2026 | 122 个搜索候选，检查 1 个、121 个未检查；一次完整预览，待人工审阅 | 观察 16:00 UTC；临时截止 13:39:20.481215 UTC |

两次搜索均首次成功，未声称覆盖整页或全部市场。实际搜索 2 次＋详情 2 次＋两份完整预览的
GET 上界 6 次，合计上界 **10／配置上限 32**；`eligibility_scan_complete=false`、
`selection_is_approval=false`、`operator_approved=false`、模型／数据库调用均为 false。

两个合约都是 Binance BTC/USDT 或 ETH/USDT 的 ET 正午一分钟 Close；参考行情是 Coinbase／Kraken
USD 小时线，窗口 09:00—12:00 UTC、3 根。来源／交易对／粒度差异已在结果中显式标注，
`source_suitability_review_required=true`、`settlement_source_verified=false`。G1 只证明可进入待审路径，
不能将其解释为来源已认证、人工已批准、真实模型已运行或结算已确认。

公开证明 artifact `10348885551` 已下载并验证；ZIP SHA256
`32614b0a3fc25962dd0d0936a7a35834f57fb4a86c178047767bf07f3f7222af`；
17689 字节日志 SHA256 `a4d2fee6aa2780f657cc20c81289ca4ba815875d8eb2ab508778dc33084b4745`。
固定证据摘要及完整条件 ID／来源哈希保留于 PR #28 的评论 `5664592502`，不依赖会过期的 artifact 才能知道结论。
原始公开响应只在内存处理，不新增文件业务历史；上述临时 cutoff 和 hash 不得复用为后续授权。

### 后续边界

WP-01 DONE 不改变 D1—D3 或其他五包状态；不降低 G2—G6。下一步是模型装配／预算的无凭据设计和
有界任务运行接线，实际模型及业务写入仍待单独授权。本轮无新迁移、依赖升级、长期 Release 或用户安装覆盖。
工程工作流用于固定代码公开验证，不进入应用运行时。没有新的本机必做项，不安排重复的本地 Agent 验收。


## 11. WP-03 持久批次与有界执行（PR #29；G3 尚未关闭）

基于 `cd74397ad87547c143dd6b926a6dc4554c34e0d4`（WP-01 已完成），复用现有任务领取、
收录和项目数据库会话，增加 [持久批次运行](docs/research-dispatch.md)。
本项是执行主流程接线，不是新增旁路报告／仪表盘，也不冒充常驻定时服务。

- 一次显式入队保存完整有界 BTC／ETH 请求；同 ID 原样重试返回原时间，改内容拒绝。
- 显式运行只启动仍新鲜且无领取记录的任务；捕获失败和 incomplete 原样保留，不按年龄重跑。
- 一轮任务／并发数有界，停止阻止后续入场，已入场工作自然结束；按原批次 ID 在新进程读回。
- 新增第 64 份迁移 `20260914000000_research_dispatch_batches.sql`；原 63 份字节不变。
  仅在隔离实例验收 63→64 和历史保全，未迁移用户实例；旧 kit 不覆盖、不自动升级。
- 实测命令：`python -m pytest -q tests/test_research_dispatch.py`；原生另显式启用
  `tests/test_project_postgres_dispatch_native.py`；完整 `scripts/verify_local.py --full`
  和适用 Windows／分发 CI 的最终固定版本证据记录在本变更 PR，失败不替换为旧版本结果。

**PR #29 时 WP-03 仍 PARTIAL；V1 仍 1／6。** 当时单批次有序限量并非跨批公平调度：
无领取的持续数据库／身份错误可能消耗下一轮额度；没有耐久错误轮转、统一启停操作入口
或共享提供商费用约束。已领取任务绝不因此重跑；这些缺口及 WP-02 预算联动完成后再判 G3。
D1—D3 未变，本变更不选择模型、不读密钥、不发送真实研究、不写用户业务数据库。


## 12. WP-03 本次推进：持久公平轮转（G3 尚未关闭）

基线：`122b0976fd7a0b10e3d3bc67a2aba4eab75f551d`（PR #29）。
本项只关闭 G3 的固定名册轮转／失败任务让位子条件，不新增第七工作包。

- 复用原批次和执行器，将 1—10 个已入队批次按位置交错，合计最多 100 个请求。
  每轮上限仍显式指定；游标在调用模型前提交到同一项目 PostgreSQL。
- 前面任务的领取前错误不永久占住下一轮额度。新轮次从持久游标继续；已经领取但无结果
  的任务仍为 incomplete，不按年龄重跑，严格评估仍阻断。
- 同轮次编号重放只返回原选择凭据，零模型执行；不把选择凭据当成已执行／已完成证明。
  停止、提交不确定、进程中断和批次读取失败分别处理，不宣称自动恢复丢失的结果。
- 新增第 65 份迁移 `20260915000000_research_dispatch_turns.sql`，原 64 份字节保全。
  仅隔离源代码实例执行 64→65 验证，旧批次和记录保持不变；不升级用户实例或覆盖旧 kit。
- 自审按固定源码核对身份／请求哈希、环形顺序、停止、幂等、并发和失败保留。
  专项命令：`python -m pytest -q tests/test_research_dispatch_rotation.py`；
  原生证明：`tests/test_project_postgres_rotation_native.py`（显式 opt-in）。
  最终提交、全量验证、Windows 和分发结果及首败记录固定在本变更合并 PR，不借用前一提交结果。

**六包状态仍 1／6；WP-03 PARTIAL。** 新轮转仅覆盖明确的不可变名册，不是全库自动调度、
常驻日历服务或全局预算。批次无法完整读取时整轮拒绝，不能靠跳过坏数据凑成功。
统一操作入口、获准模型／费用联动和最终 G3 组合验收仍未完成；D1—D3 未变。
后续应接 WP-02／WP-03 的受控操作路径，不再增加旁路诊断或重写研究框架。


## 13. WP-02／WP-03 本次推进：共享模型调用预算预留（G2／G3 尚未关闭）

基线 `16b7594964d424cce51c7e7afb20ed7eecbbe054`（PR #30）。本项将
[同库调用额度预留](docs/research-model-budget.md)接入原单任务／批次／轮转执行路径，
不是新增旁路诊断、提供商选择或密钥读取。

- 显式不可变预算限定具体请求哈希、模型标签、过期时刻、调用数、消息字节和输出 token；
  以整数货币微单位保守预留整次费用上界，不把响应后的用量统计当成发送前限额。
- 每次实际客户端入口前提交额度；多个工作者共享同一额度，重放凭据不再次执行；
  超时、错误、确认丢失或进程中断都不自动退额。预算已空／已过期不领取新任务。
- 只在已有任务领取后使用惰性客户端；未完成记录、失败收录和原严格评估规则保留。
  预算中途耗尽可留下原有 `model_failed` 结果；原 `model_calls` 不是实际发出的收费请求计数。
- 新增第 66 份迁移；原 65 份不改。仅隔离源代码实例验证 65→66、并发上限、断进程后
  额度不返还及旧记录保全。旧 63→64、64→65 验证仍针对各自原目标，不升级用户实例。
- 专项 `tests/test_research_model_budget.py` 和显式原生
  `tests/test_project_postgres_budget_native.py`，以及完整离线／Windows／分发验收的
  最终提交和首败记录固定在本变更 PR；未执行的检查不能用模拟结果代替。

**V1 仍 1／6，WP-02 AWAITING_OWNER，WP-03 PARTIAL。** 这是可执行的内部预留额度，
不是提供商账单上限认证：真实适配器须保证每次调用不隐含重试或额外操作，收费上界必须
覆盖所有费用并得到独立验证。当前没有选定／验证真实提供商或费率；`cost_bound_attested`
和参考哈希是调用方声明，不是身份或价格真伪认证。D1—D3 未明确前不运行真实研究。
WP-03 的统一操作入口、实际获准模型／计费联动及最终组合验收继续待办。

WP-06 继续保留 PR #30 的 Windows PowerShell 5.1 首次启动／管道超时问题：
同提交第二次通过不等于根因消失，不允许预热、扩大超时或静默重试到绿来关闭最终验收。


## 14. WP-03 本次推进：统一受控任务入口（G3 尚未关闭）

基线 `038be0c0df91999e6038e8fefe2830022b2780fb`（PR #31）。
复用[批次／轮转操作说明](docs/research-dispatch.md)及现有托管会话，增加
`scripts/manage_research_tasks.py` 和 `research_dispatch_cli.main`；不是另一套队列、
报表、调度服务或凭据配置器。查询原批次／轮次／预算，显式运行一轮必须同时给出
已创建预算、模型调用许可和应用提供的获准客户端工厂。独立脚本没有客户端配置，
拒绝实际运行；不把无凭据的接线冒充真实提供商接入完成。

- 同轮次重放不执行；新轮次从持久游标继续。停止后未入场项仍在原批次，
  已领取但无结果项保留 incomplete，不按年龄、换编号或换预算重跑。
- 只有托管会话清理成功后才输出原元数据回执。失败、捕获失败、缺失结果和中断
  不被“dispatched”或退出码 0 混同为全部完成；异常后明确保留可能已调用／已写入。
- 不输出原始输入、模型内容或 pending_run；需要保留原结果作 capture-only 恢复时
  使用既有类型化会话 API，不能从控制台回执重建预测。原严格评估门槛不变。
- 无迁移或依赖变更；新脚本纳入新构建包的可选入口，不改旧包清单，不覆盖用户安装。
- 同一协调者单独自审先复现六项 SystemExit 绕过失败状态的反例，再修复异常边界；
  保留首败而不削弱断言。专项命令：`python -m pytest -q tests/test_research_dispatch_cli.py tests/test_research_dispatch_cli_review.py`。
  原生组合证明：`tests/test_project_postgres_dispatch_cli_native.py`（显式 opt-in），
  覆盖两团队多轮、停止重启、失败留存、真实子进程中断和额度不返还；输入／客户端仍为合成。
- 精确最终 head／tree、独立工作副本自审、全量离线及 Windows／分发 CI 结果和未执行项
  固定记录在本变更 PR；不得用旧版本通过记录代替当前最终门禁，也不宣称第三方审核。

**WP-03 仍 PARTIAL，WP-02 AWAITING_OWNER，V1 仍 1／6。** 本项只关闭受控操作入口
子任务；D1—D3、真实适配器与收费上界核验、最终获准模型组合验收仍待完成。
WP-06 的 PR #30 PowerShell 5.1 首跑超时根因仍未关闭。没有真实模型调用、用户业务库
读取／迁移、密钥发现、长期 Release 或实盘操作；不派发重复的本地验收。


## 15. WP-04 本次推进：原预测绑定的人工结算确认（G4 尚未关闭）

基线 `62a6337d0484d3dd2cc457a6b2d468282a33204e`（PR #32）。
复用[结算清单命令](docs/research-resolution-queue.md)、原始执行／候选查询、既有合约／观察时刻门禁
和原子结算保存接口，增加 `--confirm --allow-resolution-write`；仅显式读取一份有界的
人工已核验 stdin 输入，不新建文件业务队列、结算表、网络采集或评分系统。

- 绑定原完成预测的记录／请求哈希及原未确认候选的编号／哈希；新旧问题／规则必须逐字一致。
  已保存失败、缺失结果、规则改动、来源描述或时刻冲突均不晋升为已结算结果。
- 声明 Binance BTCUSDT／ETHUSDT、1m、Close 必须匹配受支持原规则；原预测先于 cutoff，
  cutoff 先于实际分钟开盘；结算不能先于收盘，人工确认不能先于候选保存。
- 来源标签、引用网址和人工布尔值不是来源真伪认证；实际 Close 和 YES／NO 仍须人工独立核验。
  原证据文本及独立哈希连同记录／候选绑定保存在既有 confirmation.source_text 的版本化封套内。
- 新审核与 outcome 用既有事务原子保存；原预测和旧候选不改。同输入同版本重放保留原时间，
  不自动重取候选／重试／覆盖结果；异常可能已提交。拒绝的 stdin 不新增耐久错误日志。
- 同一协调者单独反例自审先复现三项“同审核编号、不同原预测／结果／来源仍显示成功”的反例，
  再补控制台回执完整绑定，不削弱断言。原生测试设计还纠正了在父会话持锁时启动子进程的问题，
  调整测试顺序而不放松生产生命周期锁。没有宣称第三方／全新 Agent 审核。
- 专项：`tests/test_research_resolution_confirmation.py` 与 `tests/test_research_resolution_confirmation_review.py`。
  原生组合：`tests/test_project_postgres_confirmation_native.py`，使用真实数据库时钟、前瞻性合成预测，
  等声明分钟实际收盘后走真实控制台、保存 YES／NO、重放重启、旧记录保全及原评估器。
  精确最终 head／tree、全量离线／Windows／分发 CI、首败和未执行项固定在本变更 PR。

**WP-04 仍 PARTIAL，G4 未关闭；V1 仍 1／6。** 真实模型／真实事件证据与独立人工核验
不能由合成测试代替。D1—D3 和 WP-06 的 PowerShell 5.1 首跑可靠性问题仍未解决。
无迁移、依赖升级、用户业务库访问、实际模型／行情调用、旧 kit 覆盖或实盘操作。


## 16. WP-05 本次推进：原研究记录到成本感知模拟评估（G5 尚未关闭）

基线 `4a6e787e1b0a6fb93c0d36455987c66f0b043e7c`（PR #33）。
在[原评估入口](docs/research-evaluation-console.md)加入显式 `--paper-stdin`，
托管 API 为 `evaluate_paper`。只读原完整执行历史，再组合最多 100 份调用方提供的
历史行情／费用场景；不新增评分器、文件业务队列、账户接口或数据写入。

- 原 first-recorded-attempt 选择和严格 incomplete 阻断不变；所有原尝试都保留，
  缺少场景、原失败、后续尝试、未决及成本／风险拒绝分别可见，不筛掉失败凑收益。
- 精确请求／记录与事件绑定，复用原合约／观察时刻门禁；显式 Yes／No 到 token 映射。
  两侧都按对应 token 的 ask 深度模拟买入，保留请求量、成交量及未成交余量。
- 复用既有撮合及成本边际计算；金额使用未舍入的深度成交额，不用三位显示均价。
  ask 已包含跨价差成本，深度影响已在成交额中，均不重复收费；费用七项显式提供。
- 单份场景收益是单位兑付模型下的假设计算，不是实盘收益、当前费率认证或投资建议。
  拒绝行的 hypothetical fill 不代表已执行；不汇总挑选场景为策略收益或独立样本。
- 单独自审先复现“结算早于实际观察收盘仍算收益”和“等价时区表示哈希不同”两项反例，
  再修复；原失败断言保留。专项 `tests/test_research_paper.py`、
  `tests/test_research_paper_review.py` 含独立 Fraction 金额对照。
  扩展原生 `tests/test_project_postgres_confirmation_native.py`，复用已有真实时钟与
  临时数据库，检查未决／已结算成本、真实控制台、重启保全和真实 incomplete 拒绝。
- 首轮完整回归发现新模式的中断处理改变了原入口契约；保留旧测试并补六项兼容反例，
  将 KeyboardInterrupt／SystemExit 的新处理仅限显式 paper 模式，非 paper 仍原样传播。
  不改旧断言或跳过失败；首轮 CI 失败与最终修复版证据分别保留。
- 精确最终 head／tree、专项／全量离线／Windows／分发结果及首败证据固定在本变更 PR。
  无迁移、依赖变化、用户业务库访问、真实行情／模型调用或旧 kit 覆盖。

**WP-05 仍 PARTIAL，G5 未关闭，V1 仍 1／6。** 这里不保存场景源数据或认证采集时间，
CLOB 原 timestamp 不被解释为已认证服务端时间；哈希不替代源材料的获准耐久存储。
获准行情／成本场景收录、真实 BTC／ETH 工程样本、独立来源／结算核验和最终组合验收仍缺。
D1—D3、原策略验证门槛及 WP-06 的 PowerShell 首跑可靠性问题不变。
