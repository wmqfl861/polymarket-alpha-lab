

## 44. WP-02：固定官方 CLI 的显式验收入口（2026-09-20）

基线 `cbf2d3b1baff46c1a9b1e5f2983984837c8e6786`，完整树
`2ff58d2edfd5b43bef7c4117b0ac5e0261c6b25f`。本轮不继续增加适配器，
为已接入的 Claude 配置补齐可交给独立测试宿主执行的固定二进制验收。

- 新增仅在 SOURCE 中运行的六场景 opt-in 测试：成功、429、500、坏动作、实际工具
  响应与截断。显式给定既有原生文件的路径、哈希、准确大小和隔离宿主声明；不下载、
  安装、登录、找密钥、连接业务库或真实提供商，未启用时明确跳过，错误配置不能跳过到绿。
- 每场景先验证版本再作一次合成请求；沿用原进程监督和严格结果校验。为本机假服务
  仅在测试内声明 HTTP loopback 覆盖，不修改生产 HTTPS 限制或声称验证了生产 TLS。
- 请求计数／动作与用量对照、无会话状态的前后快照均有界。新增／改写／无法完整检查
  的状态文件会失败；不通过删除日志、忽略错误、放宽格式或换版本制造通过。
  快照不是瞬时写入／外部网络监控，也不是文件沙箱；这些未知项始终不标为已认证。
- 合成 Python 子进程验证测试装置本身，不冒充官方 CLI。独立自审先复现六项测试装置
  误判／读取边界反例，再修正；已有源码、68份SQL、依赖、任务身份及业务时限均不变。
  Windows 只追加两个测试模块和触发路径，原选择与时限保留。最终冻结源码、自审和
  完整托管结果以关联 PR 的确切提交／原日志为准，通过前不合并。

**官方 Claude 二进制本轮未执行。** 当前环境没有该程序，先前下载限制继续遵守，
不换镜像、软件包、CI代下载或其他身份绕过。真正缺失的是在既有、来源可验证的
二进制与无凭据／禁外网的独立宿主中执行此固定批次，而不是用户重新决定 D1—D3。
交接按 GitHub 固定源码、校验值和完整提示词进行；不要求重复数据库／网络旧验收。
任何前提缺失或六场景失败都保留 BLOCKED，不在普通用户业务安装中临时补做。

本单元完成只表示验收入口和测试装置可交付，**G2—G6仍未关闭，V1仍1／6**。
不以合成测试解除 Codex SQLite 阻断或批准 Claude 的真实持久化／网络／模型身份。
