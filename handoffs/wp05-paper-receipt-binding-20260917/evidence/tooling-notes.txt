Tooling/provenance notes, separate from product failures:
- The GitHub connector exposed 48 read operations and no commit/PR write actions in this continuation. Plugin discovery identified the installed GitHub integration, not a separate available writer. Remote Desktop Commander reported no connected devices. A normal public git ls-remote failed DNS resolution; raw/API host resolution also failed. No credential lookup or alternate identity was attempted.
- Reconstructed the 6461 tracked base blobs from the pinned PR52 source archive, verifying Git blob hashes, full tree and source commit. Copied only hash-checked tracked source from the already-downloaded PR53 kit; never executed or installed its runtime. Initially the inspection looked for PROJECT-BUNDLE.json at the ZIP root; the archive actually has a polymarket-alpha-lab/ prefix. This failed before overlay; the corrected read validated every manifest payload.
- Remaining three PR53 files were reconstructed to their recorded Git blob hashes. The resulting full tree is exactly 4d15e1d555f108e19edf19e160593eea2841b28c, the upstream a71ae4d... tree. Local commits are explicitly bookkeeping, not fake upstream commits/ancestry.
- Disposable venvs reference the existing container build/test packages; editable installation succeeded with no project dependency changes. They are not a locked hosted Windows environment. No CodeGraph execution is claimed.
- The separate receipt byte-comparison runner initially lacked the source tests package on sys.path and failed before any comparison. Supplying its SOURCE root for that audit runner produced 24 comparisons. Production files and test assertions were not modified for this setup correction.
- Native child code was compiled and exercised through two synthetic offline wrapper invocations, without an OS child, engine or real database. Native acceptance is still unexecuted, and no runtime result is inferred from those wrapper checks.
- The preflight patch worktree intentionally retains exactly six staged modifications after git apply --check --index and application; its result tree matches the frozen candidate. The development and detached review worktrees are separate and clean. No user worktree, .local, database or installation was touched.
